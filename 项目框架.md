# 项目框架

| 项 | 内容 |
| --- | --- |
| 版本 | v0.1 |
| 更新日期 | 2026-09-23 |
| 对应进度 | tasks.md 的 T001 ~ T025 已完成（**US-1、US-2 完成**：规范 → 逐条确认 → 导出评审稿 → 拆成任务 → 覆盖检查 → 就绪排序） |
| 维护方式 | 与代码改动**同一个提交**里更新，见 §0 与 §6 |

---

## 0. 这份文档是什么，怎么维护

**记录什么**：每个源码文件里有哪些类与函数、各自负责什么、文件之间谁依赖谁。

**不记录什么**：实现细节（看代码）、需求（看 [spec.md](./spec.md)）、设计与取舍理由（看 [plan.md](./plan.md)）。三者分工要守住，否则四处重复、四处不同步。

**维护规则**：

1. 改动 `src/pm_agent/` 下的代码，**同一个提交里**更新本文件。
2. **新增、删除、改名**类或函数时必须同步；只改函数体内部实现而签名不变时，可以不动。
3. 判断标准只有一条：**别人只读本文件，能不能知道该去哪个文件找哪个函数。**
4. 顶部"更新日期"和"对应进度"跟着改；改完过一遍 §6 的检查表。

---

## 1. 分层与依赖方向

依赖方向只有一条：**上层依赖下层，下层不知道上层**。这条线一旦破了，改动会四处牵连。

### 已经成立、值得保持的三条约束

1. `errors.py` 不依赖任何内部模块 —— 所以每一层都能安全地 import 它。
2. `workspace/format.py`、`model/base.py`、`templates/__init__.py` 不依赖任何内部模块 —— 它们是纯零件。
3. **`stages/__init__.py`（阶段说明）只依赖 `errors.py`** —— 它不知道"项目工作区"和"模型"的存在。这条是分层是否干净的试金石：阶段说明是**内容**，不该知道数据存在哪、模型是谁。

   T011 在这一层上开了一个口子，但开得有界限：`stages/` 里**新增了一个 `specify.py`**，它要调模型、要写文件，所以依赖 `model` 和 `workspace`。**界限在文件级**——`import pm_agent.stages` 仍然不会把模型层拉进来，`__init__.py` 也仍然只认 `errors`（见 §2 的分节说明）。

### 各文件的内部依赖

| 文件 | 依赖的内部模块 |
| --- | --- |
| `__init__.py` | （无） |
| `__main__.py` | `cli` |
| `errors.py` | （无） |
| `cli.py` | `errors`、`model`、`workspace`、`workspace.requirements`、`workspace.review`、`workspace.tasks`、`stages`、`stages.specify`、`stages.tasks` |
| `stages/__init__.py` | `errors` |
| `stages/specify.py` | `errors`、`model`、`prompts`、`workspace`（format / changes / files / store） |
| `stages/tasks.py` | `errors`、`model`、`prompts`、`workspace`（format / changes / files / store / tasks） |
| `prompts/__init__.py` | `templates` |
| `workspace/__init__.py` | `workspace.store`、`workspace.handoff`、`workspace.changes`、`workspace.requirements`、`workspace.review`、`workspace.tasks` |
| `workspace/format.py` | （无） |
| `workspace/files.py` | `errors` |
| `workspace/handoff.py` | `errors`、`workspace.format`、`workspace.files`、`workspace.store` |
| `workspace/changes.py` | `errors`、`workspace.format` |
| `workspace/requirements.py` | `errors`、`workspace.format`、`workspace.changes`、`workspace.files`、`workspace.store` |
| `workspace/review.py` | `errors`、`workspace.format`、`workspace.changes`、`workspace.files`、`workspace.store` |
| `workspace/tasks.py` | `errors`、`workspace.format`、`workspace.changes`、`workspace.store` |
| `workspace/store.py` | `errors`、`templates`、`workspace.format`、`workspace.changes` |
| `model/__init__.py` | `errors`、`model.base`、`model.echo`、`model.openai_compat` |
| `model/base.py` | （无） |
| `model/echo.py` | `model.base` |
| `model/openai_compat.py` | `errors`、`model.base` |
| `templates/__init__.py` | （无） |

---

## 2. 源码文件清单

### `src/pm_agent/__init__.py`

**职责**：包的身份信息。**依赖**：无。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `__version__` | 常量 | 版本号，`pm-agent version` 显示它 |

### `src/pm_agent/__main__.py`

**职责**：让 `python -m pm_agent` 可用（没装命令行入口时的退路）。**依赖**：`cli.app`。

### `src/pm_agent/errors.py`

**职责**：定义所有"可预期失败"。每个错误都能带一句 `hint`，即"接下来该怎么办"（FR-037）。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `PMAgentError` | 基类 | 所有可预期失败的父类，带 `message` 与 `hint` |
| `WorkspaceError` | 异常 | **项目工作区**相关的问题：找不到、打不开、结构不对 |
| `FormatError` | 异常 | 工作区里的文件内容不符合格式定义 |
| `ModelConfigError` | 异常 | 模型接入配置不完整（缺密钥、超时值不是数字等） |
| `ModelError` | 异常 | 模型调用失败（网络、鉴权、返回结构异常） |

> 分层的意义：`stages/` 里的内置数据坏了用 `PMAgentError`，工作区文件坏了用 `FormatError`。错误类型编的就是"问题出在哪一层"。

### `src/pm_agent/cli.py`

**职责**：命令行入口，只做三件事——解析参数、调下层、把结果排版成给人看的样子。**依赖**：`errors`、`model`、`workspace`、`stages`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `app` | 常量 | Typer 应用，命令注册在这里 |
| `console` | 常量 | Rich 输出通道 |
| `label(tag, style, body)` | 函数 | 拼"标签 + 内容"一行；内容不做 markup 解析 |
| `guarded(func)` | 装饰器 | 捕获 `PMAgentError` → 打印"错误 + 怎么办"、退出码 1 |
| `version()` | 命令 | 显示版本号 |
| `init(path, name, goal, learning_goal, git)` | 命令 | 创建项目工作区；已存在文件一律跳过 |
| `show(path)` | 命令 | 项目概览：元信息、学习目标、需求条目数、任务完成数 |
| `requirements(path, limit)` | 命令 | 列出规范里的需求条目（编号 / 分组 / 内容） |
| `tasks(path, milestone, ready, coverage)` | 命令 | 列出任务；`--ready` 看现在能动手的，`--coverage` 看覆盖缺口 |
| `breakdown(path, provider, yes)` | 命令 | 调模型把规范拆成任务 → 覆盖检查 → 预览 → 确认后写入 |
| `check(path, fix)` | 命令 | 检查工作区格式；`--fix` 补建缺失的数据目录 |
| `ask(prompt, provider, system)` | 命令 | 调一次模型，验证接入是否配好 |
| `stage(name)` | 命令 | 打印一个阶段的目的 / 输入 / 预期产出 |
| `specify(path, goal, provider, yes)` | 命令 | 调模型生成规范 → 打印预览 → 确认后写入 `spec.md` |
| `history(path, limit, requirement)` | 命令 | 列出变更记录；加 `-r FR-003` 看某条需求的来龙去脉 |
| `undo(path)` | 命令 | 撤回最近一次变更，并打印恢复了什么 |
| `questions(path)` | 命令 | 列出规范里还没答案的问题（待澄清） |
| `confirm(id, path)` | 命令 | 确认一条需求看过、可以照它做 |
| `review(path)` | 命令 | 导出评审稿到 `reports/` |
| `_titled(title, subtitle)` | 内部 | 面板标题的富文本拼装 |
| `_show_requirement_history(project, id)` | 内部 | 打印某条需求的来龙去脉；没有变更记录时如实说明 |
| `_git_summary(status)` | 内部 | 把 git 初始化结果翻译成"发生了什么 + 用什么颜色" |

### `src/pm_agent/stages/__init__.py`

**职责**：把内置的阶段说明（`stages.yaml`）变成人能读的三行。**不调模型、不写文件、不碰项目工作区。** **依赖**：仅 `errors`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `Stage` | frozen 数据类 | 一个阶段的说明：`title` / `purpose` / `inputs` / `outputs` |
| `_stages()` | 内部 | 读 `stages.yaml`、校验、返回有序的 `(key, Stage)`；带 `lru_cache` |
| `load_stages()` | 函数 | 返回全部阶段（只读视图 `Mapping[str, Stage]`） |
| `get_stage(key)` | 函数 | 按名字取一个阶段；未知名字抛 `PMAgentError`，`hint` 里列出可选项 |
| `announce(key)` | 函数 | 渲染成三行文本；**只返回字符串，不打印** |

> `stages.yaml` 的四个字段缺一不可，校验失败会指出"哪个阶段、哪个字段"。

> **说明层与实现层分在文件级**：`__init__.py` 只做"读说明、渲染三行"（依赖仅 `errors`）；具体某一阶段怎么干活放在 `specify.py` 这样的文件里。所以 `import pm_agent.stages` 不会把模型层和文件系统拉进上下文——这条在 T011 之后依然成立。

### `src/pm_agent/stages/specify.py`

**职责**：specify 阶段怎么干活（T011）：把 `project.yaml` 里的目标交给模型，产出一份规范草稿。**只产出数据**——不打印、不询问、不写文件，预览与确认归 CLI。**依赖**：`errors`、`model`、`prompts`、`workspace`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `REQUIRED_TOPICS` | 常量 | FR-002 要求规范至少出现的五项关键词：目标 / 使用者 / 范围 / 成功标准 / 待澄清 |
| `MIN_LENGTH` / `RAW_EXCERPT` | 常量 | 判定"不像规范"的最短长度；报错时附带多少字模型原文 |
| `SpecDraft` | frozen 数据类 | 一份**尚未落盘**的规范草稿：`text` + `source`（哪个模型产出的） |
| `draft_spec(project, provider, goal=)` | 函数 | 组装提示词、调一次模型、清理输出；**只读项目** |
| `check_draft(draft)` | 函数 | 返回全部问题；**回声实现的输出会被专门拦下**，否则它会把提示词原样当规范 |
| `compose_spec(project, draft)` | 函数 | 草稿 + **保留原有的 frontmatter** → 最终 spec.md 文本 |
| `prepare_spec_change(project, draft, reason=)` | 函数 | 校验 + 组装成待确认的 `Change`；**不合格就抛错、不产出变更** |
| `_clean` / `_strip_code_fence` | 内部 | 剥掉模型自作主张加的 frontmatter 与代码围栏 |

> **校验不过就没有东西可写**：`prepare_spec_change` 不合格时直接抛错、不返回 `Change`，所以调用方没有任何机会把半成品写进规范（FR-037）。

### `src/pm_agent/stages/tasks.py`

**职责**：tasks 阶段怎么干活（T018）：把规范里的需求条目交给模型，产出一份任务清单草稿。和 specify 是兄弟——只产出数据、先校验再落盘、提示词放成数据文件。**依赖**：`errors`、`model`、`prompts`、`workspace`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `MIN_TASKS` | 常量 | 草稿至少要解析出几条任务 |
| `TaskDraft` | frozen 数据类 | 一份**尚未落盘**的任务清单草稿：`text` + `source` |
| `draft_tasks(project, provider)` | 函数 | 组装提示词（含需求清单）、调一次模型、清理输出；**只读项目** |
| `check_draft(project, draft)` | 函数 | 挡住写入的那部分校验：解析不出任务、任务缺三要素 |
| `prepare_tasks_change(project, draft, reason=)` | 函数 | 校验 + 产出待确认的 `Change`；不合格就抛错、不产出变更 |
| `draft_coverage(project, draft)` | 函数 | 草稿的覆盖缺口——**只报告，不挡写入**（FR-010） |

> 校验分两层，边界要分清：**缺要素挡住写入**（写进去就是坏清单），**覆盖缺口只报告**（留着让人决定怎么补）。

### `src/pm_agent/prompts/__init__.py`

**职责**：提示词（作为包数据分发）。提示词是**内容**不是逻辑，所以放文件里：改它不用碰代码，本身也能被人评审。**依赖**：`templates`（共用同一套占位符规则）。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `load(name)` | 函数 | 读取原始提示词文本 |
| `render(name, context)` | 函数 | 渲染占位符；规则复用 `templates.render_text` |

> 每个阶段一对文件：`*.system.md` 是**规则**（你是谁、必须遵守什么），`*.user.md` 是**数据**（这次要处理什么）。分开写不只是整洁——使用者给的目标属于"数据"，放进 system 里就可能被当成指令。

### `src/pm_agent/workspace/__init__.py`

**职责**：对外暴露工作区读写层的公共接口。**依赖**：`workspace.store`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `Project`、`create_project` | 重导出 | 供上层 `from pm_agent.workspace import ...` |

### `src/pm_agent/workspace/format.py`

**职责**：项目工作区的**格式定义与校验规则**。格式写在这里，人可读的模板放在 `templates/`，两处必须一致。**依赖**：无。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `PROJECT_FILE` / `SPEC_FILE` / `PLAN_FILE` / `TASKS_FILE` | 常量 | 四个固定文件名（`project.yaml` / `spec.md` / `plan.md` / `tasks.md`） |
| `REQUIRED_FILES` / `OPTIONAL_FILES` | 常量 | 必需与可选文件 |
| `HISTORY_DIR` | 常量 | 变更历史目录 `history`——`changes.py` 用它放可撤回的快照 |
| `DATA_DIRECTORIES` | 常量 | 六个数据目录：`evidence` / `decisions` / `sessions` / `reports` / `skills` / `history` |
| `PROJECT_STATUSES` | 常量 | 项目状态：`active` / `paused` / `done` |
| `SESSION_STATUSES` | 常量 | 交接记录状态：`ok` / `conflict` / `aborted`（`handoff.py` 用它校验） |
| `LEVEL_ERROR` / `LEVEL_WARN` | 常量 | 两级严重度：`error` 阻止打开，`warn` 只提示 |
| `Problem` | 数据类 | 一条校验问题：`where` + `message` + `fix` + `level`，`render()` 拼成一行 |
| `ProjectMeta` | 数据类 | `project.yaml` 的结构化表示 |
| `REQUIREMENT_LINE_RE` / `SUSPECT_ENTRY_RE` / `HEADING_RE` | 常量 | 需求条目的识别规则：合规条目、**疑似条目但格式不合规**、正文标题（用来判断条目归哪个分组） |
| `WATERMARK_KEY` | 常量 | frontmatter 里的编号水位线字段名 `next_requirement` |
| `CONFIRMED_KEY` | 常量 | frontmatter 里记录已确认条目编号的字段名 `confirmed` |
| `CLARIFICATION_MARKER` | 常量 | 待澄清标记 `[待澄清]` |
| `Clarification` | frozen 数据类 | 一处待澄清：`text`（问题）+ `source`（来自哪条需求或哪一节）+ `line` |
| `parse_clarifications(text)` | 函数 | 扫描 `[待澄清]` 标记；**只读取，不生成、不替人回答**（FR-004） |
| `Requirement` | frozen 数据类 | 一条需求：`id` / `text` / `section`（所属分组）/ `line`（行号，定点修改要用） |
| `TASK_LINE_RE` / `SUSPECT_TASK_RE` / `TASK_FIELD_RE` / `TASK_REFERENCE_RE` | 常量 | 任务行的识别规则：合规行、疑似行、``**字段**：值``、开头的 `（…）` 引用 |
| `FR_ID_RE` / `SC_ID_RE` / `TASK_ID_RE` / `REQUIRED_TASK_FIELDS` | 常量 | 引用里挑需求编号 / 成功标准编号 / 任务编号；FR-009 要求的三要素标签 |
| `Task` | frozen 数据类 | 一条任务：`id` / `title` / `done` / `parallel` / `milestone` / `reference` / `fields` / `requirements` / `criteria` / `standard` / `priority` / `depends_on` / `evidence` / `line`，另有 `has_source` 判"交代过来源没有"（需求、成功标准、或明确的 `—` 都算） |
| `parse_tasks(text)` | 函数 | 解析任务清单；只认 `- [ ] **T001**`（含 `[P]`）这种行 |
| `check_tasks(text)` | 函数 | 编号重复、编号不合规、**依赖悬空**——都是 `warn` |
| `parse_requirements(text)` | 函数 | 解析出全部条目（**只认 `- **FR-xxx**` 那种行**，正文里的引用不算条目） |
| `check_requirements(text)` | 函数 | 编号重复、疑似条目但格式不合规；两条都是 `warn`——有问题但不让项目打不开 |
| `new_project_meta(...)` | 函数 | 按当前格式生成一份元信息 |
| `coerce_iso_date(value)` | 函数 | 把日期统一成 `YYYY-MM-DD`；同时接受字符串与 YAML 解析出的日期对象 |
| `validate_meta(data)` | 函数 | 校验 `project.yaml` 内容，返回**全部**问题（不提前退出） |
| `meta_from_dict(data)` | 函数 | 已通过校验的字典 → `ProjectMeta` |
| `errors(problems)` / `warnings(problems)` | 函数 | 按严重度筛选 |
| `render_problems(problems)` | 函数 | 拼成多行文本 |
| `check_workspace(root)` | 函数 | **只读**检查整个工作区，返回问题清单 |
| `_is_nonempty_str` / `_read_text` / `_validate_project_text` / `_split_task` | 内部 | 工具函数；`_split_task` 把一行任务拆成"引用 + 字段 + 动作" |

### `src/pm_agent/workspace/files.py`

**职责**：Markdown + YAML frontmatter 的读写。**依赖**：`errors`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `FRONTMATTER_FENCE` | 常量 | 分隔符 `---` |
| `split_frontmatter(text)` | 函数 | 拆出 `(元数据, 正文, 是否有 frontmatter)`；没有 frontmatter 不算错 |
| `read_markdown(path)` | 函数 | 读文件 → `(元数据, 正文)` |
| `render_markdown(meta, body)` | 函数 | 拼成"frontmatter + 正文"的文本 |
| `set_frontmatter_line(text, key, value)` | 函数 | 在 frontmatter 里设一个键（**行级手术**，其余逐字不动）；没有 frontmatter 就补一个。水位线与确认名单都用它 |

> 交接记录、决策记录都是这个形态：**人能读的正文 + 程序能读的元数据**。

### `src/pm_agent/workspace/handoff.py`

**职责**：交接记录的格式、校验与读写。记录落在项目工作区的 `sessions/` 里，一个会话一个文件，**文件名就是会话键**（`YYYY-MM-DD-HH-MM-SS`，定长、可排序、不含冒号）。**依赖**：`errors`、`workspace.format`、`workspace.files`、`workspace.store`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `SESSION_KEY_FMT` / `SESSION_KEY_RE` | 常量 | 会话键的格式与识别规则 |
| `SECTION_TITLES` | 常量 | 段落定义的**唯一定义处**：字段名 → 中文标题，**字典顺序即渲染顺序** |
| `REQUIRED_SECTIONS` | 常量 | FR-016 要求的三要素字段名：`state` / `next_steps` / `open_questions` |
| `Handoff` | frozen 数据类 | 一份记录：`session`/`created`/`project`/`status` + 三要素 + `did`/`references`/`prev`（可空） |
| `check_handoff(handoff, session_key=, unknown_titles=)` | 函数 | 返回全部问题：status 取值、三要素非空、session 与文件名是否一致；`unknown_titles` 列出正文里认不出的 `##` 标题，**排在"缺内容"之前报**（标题写错常是缺内容的成因） |
| `render_handoff(handoff)` | 函数 | 渲染成「frontmatter + 正文」 |
| `parse_handoff(text, session_key=)` | 函数 | 解析并校验一份记录文本 |
| `list_handoffs(project)` | 函数 | 全部记录，最新在前——**按文件名字符串排序 = 按时间排序** |
| `read_handoff(project, key)` | 函数 | 读指定的一份 |
| `read_latest_handoff(project)` | 函数 | 读最新的一份；一份都没有时返回 `None` |
| `new_handoff(project, ...)` | 函数 | 按当前时间组装：自动填 `session`/`created`/`project`/`prev`；同秒冲突**往后挪一秒**，不使用 `-1` 后缀；`prev` **只取文件名、不解析旧记录** |
| `write_handoff(project, handoff)` | 函数 | 写入；**已存在同名记录时拒绝覆盖** |
| `_require_valid` / `_split_sections` | 内部 | 统一抛错与正文分节 |

> 边界提醒：`prev` 可空（第一份记录没有前驱）；三个必需段落不能留空——没有进展要如实写"无进展"（FR-016 / FR-033）。断链检测（`prev` 指向的记录被删）留给 T029。

> **读校验与写门禁分开**：读一份坏记录要报错，但写新记录不解析旧记录——坏记录恰恰是最需要继续工作的时候，不该被它拦住。

### `src/pm_agent/workspace/changes.py`

**职责**：写入前预览与撤回（T008）。**预览是数据，不是交互**——这一层只产出 `Change`，不打印、不询问，显示与确认归 CLI（和 T006 的 `announce` 同一个道理）。想落盘就必须先有 `Change`，所以"先出预览"不是纪律，是类型。**依赖**：`errors`、`workspace.format`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `STAMP_FMT` / `STAMP_RE` | 常量 | 变更记录目录名的时间戳格式 |
| `META_FILE` / `BEFORE_DIR` | 常量 | 记录布局：`meta.yaml` + `before/`（按原相对路径存快照） |
| `ChangeEntry` | frozen 数据类 | 变更里的一个文件：`path` + `before_bytes`（**原始字节**，None = 原本不存在）+ `after`；另有 `before_text`（仅供预览）/ `after_bytes` / `changed`（**按字节比**）/ `kind` / `diff_stat` / `snippet()` |
| `Change` | frozen 数据类 | 一份**尚未落盘**的变更：若干 `ChangeEntry` + `reason`；`changed_entries` 挑出真有变化的、`is_noop` 判断是否白跑，`render()` 产出给人看的预览文本 |
| `ApplyResult` / `UndoResult` | frozen 数据类 | 落盘与撤回的结果 |
| `prepare_write(project, rel, text, reason=)` | 函数 | 读现状、产出 `Change`，**不碰磁盘** |
| `apply_change(project, change, moment=)` | 函数 | 先存快照与 meta，再按字节写入；无实际变化则什么都不做 |
| `undo_last(project, moment=)` | 函数 | 撤回最近一次未撤回的变更；原本不存在的文件**删掉** |
| `history_entries(project)` | 函数 | 全部变更记录目录，最近在前 |
| `read_change_meta(entry_dir)` | 函数 | 读一条记录的 `meta.yaml` |
| `_normalize` / `_new_entry_dir` / `_write_backups` / `_write_meta` / `_write_meta_file` / `_is_undone` | 内部 | 路径归一、建时间戳目录、写快照与 meta、判断某条记录是否已撤回 |

> **一切都按字节**：备份存原始字节、恢复写回原始字节。Windows 上文本模式会把 `\n` 翻译成 `\r\n`，那样"撤回后与写入前完全一致"就成了一句空话。

### `src/pm_agent/workspace/requirements.py`

**职责**：需求条目的**操作**（T012）：分配编号、新增、定点修改。解析与校验在 `format.py`（那才是"格式定义"待的地方），这里只放会改文件的部分，所以它产出 `Change`。**依赖**：`errors`、`workspace.format`、`workspace.changes`、`workspace.files`、`workspace.store`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `next_requirement_id(spec_text)` | 函数 | 下一条编号：取**frontmatter 水位线**与现有最大号的较大者加一 |
| `add_requirement(project, text, reason=)` | 函数 | 产出一份"新增一条需求"的变更；插在最后一条之后，并把水位线推进 |
| `update_requirement(project, id, text, reason=)` | 函数 | 产出一份"改写某一条"的变更；**只重写那一行** |
| `RequirementRevision` | frozen 数据类 | 某条需求的一次变化：`time` / `reason` / `before` / `after`（None = 还不存在 / 被删掉） |
| `requirement_history(project, id)` | 函数 | 用 `history/` 里的 spec 快照还原"何时变、为何变"（FR-005） |
| `_with_watermark` / `_text_of` / `_spec_timeline` | 内部 | 把水位线写进 frontmatter、取某条需求的文本、把历次 spec 快照排成时间线 |

> **编号只增不复用**：删掉编号最大的那条以后，下一个新条目仍然拿新号。复用编号会让别处已有的引用静默指向另一条需求——这类 bug 不报错、最难查。

> **改动是定点替换**：为了让"其余部分逐字不动"站得住，这里用行级手术而不是"解析后重新渲染"，后者会把空行、缩进一起重排。

### `src/pm_agent/workspace/review.py`

**职责**：规范的**评审**——确认状态与评审稿导出（T015 / T016）。确认状态记在 `spec.md` 的 frontmatter（`confirmed`），和 status / version / next_requirement 一起，同属"程序维护的元数据、正文归模型产出"。**依赖**：`errors`、`workspace.format`、`workspace.changes`、`workspace.files`、`workspace.store`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `REVIEW_DIR` | 常量 | 评审稿落在 `reports/`（六个数据目录之一） |
| `parse_confirmed(spec_text)` | 函数 | frontmatter 里的已确认条目编号 |
| `unconfirmed_requirements(project)` | 函数 | 还没确认过的条目 |
| `confirm_requirement(project, id, reason=)` | 函数 | 产出一份"确认某条"的变更；**已确认过就返回无变化的变更**（上层照 T008 的规矩处理即可） |
| `build_review_document(project, exported_on=)` | 函数 | 生成评审稿：待确认条目 + 待澄清问题 + 规范全文 |
| `export_review(project, moment=, reason=)` | 函数 | 产出一份"写入评审稿"的变更，落到 `reports/评审稿-<日期>.md` |

> 确认与提意见是两件事：**确认**是"看过了、可以照它做"（写 frontmatter），**提意见**是"这条要改"（走 T012 的定点修改）。分开之后，"哪些还没看"一眼就能数出来。

### `src/pm_agent/workspace/tasks.py`

**职责**：任务的操作——覆盖缺口、最小切片、要素校验、完成与证据（T018 ~ T024）。解析与校验在 `format.py`，这里只放会改文件的部分。**依赖**：`errors`、`workspace.format`、`workspace.changes`、`workspace.store`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `tasks_by_milestone(project)` | 函数 | 按里程碑分组（FR-011）——里程碑就是二级标题 |
| `CoverageReport` | frozen 数据类 | 覆盖缺口的两个方向；`ok` 判断有无缺口，`render()` 出文本 |
| `coverage_gaps(project)` | 函数 | 未覆盖的需求 + **漏写来源**的任务（写了 `（— / —）` 不算缺口） |
| `ready_tasks(project)` | 函数 | 现在就能动手的：未完成且依赖都已完成，按优先级排（FR-013） |
| `add_task(project, ...)` | 函数 | 新增任务；**三要素缺一就不产出变更**（FR-009），依赖与需求都必须真实存在 |
| `next_task_id(tasks_text)` | 函数 | 下一个任务号：现有最大号加一（任务号不设水位线，理由写在函数里） |
| `update_task(project, id, ...)` | 函数 | 定点改某个字段；改依赖当场查悬空与自依赖（FR-012） |
| `complete_task(project, id, evidence=)` | 函数 | 标记完成；**没有证据就不产出变更**（FR-014） |
| `_priority_rank` / `_insert_position` / `_set_field` | 内部 | 优先级排序键、插入位置、行内定点改字段 |

> **创建时严格，读取时宽容**：新建任务必须带齐三要素；但读一份手写的旧清单时，缺字段只是"没写"，不该让项目打不开。

### `src/pm_agent/workspace/store.py`

**职责**：工作区读写层，也就是对外的门面。两条约定——**读之前先校验**、**写必须先准备**（没有"直接写"这个动作了）。**依赖**：`errors`、`templates`、`workspace.format`、`workspace.changes`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `Project` | 数据类 | 一个已打开、已通过校验的项目工作区 |
| `Project.open(root)` | 类方法 | 打开项目；有 error 级问题时失败并列出全部问题 |
| `Project.path(*parts)` | 方法 | 项目内相对路径；**拒绝越出项目目录** |
| `Project.read_text(*parts)` | 方法 | 读项目内文件 |
| `Project.spec_text()` | 方法 | 读 `spec.md` |
| `Project.requirements()` | 方法 | 规范里的需求条目（按出现顺序，真正解析过） |
| `Project.requirement_ids()` | 方法 | 需求条目编号（去重、按编号排序） |
| `Project.tasks_summary()` | 方法 | 任务 `(总数, 已完成数)`；正式解析在 T018 |
| `Project.prepare_write(rel, text, reason=)` | 方法 | 委托 `changes.prepare_write`：产出预览，不碰磁盘 |
| `Project.apply(change)` | 方法 | 委托 `changes.apply_change`：落盘并留可撤回的快照 |
| `Project.undo_last()` | 方法 | 委托 `changes.undo_last`：撤回最近一次变更 |
| `load_project_meta(path)` | 函数 | 解析 `project.yaml` |
| `CreationResult` | 数据类 | `create_project` 的结果：建了什么、跳过了什么、git 状态 |
| `create_project(...)` | 函数 | 增量创建项目；**已存在文件一律跳过**；生成完自检模板与格式是否一致 |
| `ensure_git_repo(path)` | 函数 | 确保是 git 版本库；做不成时如实说明，不静默跳过 |
| `_write_if_absent` | 内部 | 只在文件不存在时写入 |

### `src/pm_agent/model/__init__.py`

**职责**：按名字取出模型实现。**依赖**：`errors`、`model.base`、`model.echo`、`model.openai_compat`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `get_provider(name=None)` | 函数 | 名字缺省时读环境变量；默认 `deepseek`；未知名字抛 `ModelConfigError` 并列出可选项 |
| `DEFAULT_PROVIDER` | 常量 | `deepseek` |

### `src/pm_agent/model/base.py`

**职责**：模型接入的统一接口。**依赖**：无。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `Message` | 数据类 | 一条对话消息：`role` + `content`，`to_dict()` 转成接口格式 |
| `ModelProvider` | 抽象基类 | 只留一个抽象方法 `complete(messages, ...) -> str`；另有 `describe()` 给使用者看的一行说明 |

> 故意只留一个方法：会话循环、上下文装配这些真正属于产品的部分，不该藏在模型实现里。

### `src/pm_agent/model/echo.py`

**职责**：不联网的回声实现，用于无密钥/无网络时联调与测试。**依赖**：`model.base`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `ECHO_MARKER` | 常量 | `[echo]`，输出里必须带，避免被当成真实结果（FR-037） |
| `EchoProvider` | 实现类 | 回显最后一条 user 消息；`is_remote = False` |

### `src/pm_agent/model/openai_compat.py`

**职责**：OpenAI 兼容接口的实现（DeepSeek 走的就是它）。只用标准库 `urllib`。**依赖**：`errors`、`model.base`。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `Preset` | 数据类 | 一个服务商的默认参数：`label` / `base_url` / `model` |
| `PRESETS` | 常量 | `deepseek`、`openai-compat` 两套预设；**换服务商 = 加一行** |
| `DEFAULT_PRESET` | 常量 | `deepseek` |
| `OpenAICompatProvider` | 实现类 | 兼容 `POST /chat/completions` 的服务 |
| `.from_env(preset_name)` | 类方法 | 预设给默认值，环境变量负责覆盖 |
| `.endpoint()` | 方法 | 拼出 `/chat/completions` 地址 |
| `.complete(...)` | 方法 | 发请求、取回复；各类失败转成带 `hint` 的 `ModelError` |
| `.describe()` | 方法 | 给使用者看的说明（用**服务商名**，不是协议名） |
| `._ssl_context()` | 内部 | 支持 `PM_AGENT_CA_BUNDLE` 自定义 CA 证书 |
| `._http_error()` / `._extract_text()` | 内部 | 把 HTTP 错误与各种畸形返回翻译成人话 |

### `src/pm_agent/templates/__init__.py`

**职责**：内置模板（作为包数据随程序分发）。**依赖**：无。

| 名称 | 类型 | 作用 |
| --- | --- | --- |
| `load(name)` | 函数 | 读取原始模板文本 |
| `render_text(text, context, source=)` | 函数 | 占位符替换的**唯一实现**，被 `render_template` 与 `prompts.render` 共用（规则只该有一处定义） |
| `render_template(name, context)` | 函数 | 读取并渲染模板；**渲染后若还有残留占位符就直接报错** |

---

## 3. 非代码文件

| 文件 | 是什么 | 被谁使用 |
| --- | --- | --- |
| `src/pm_agent/stages/stages.yaml` | 四个阶段的说明**内容**（不是代码） | `stages/_stages()` 读取并校验 |
| `src/pm_agent/templates/spec.md` | 新项目的规范模板 | `create_project()` → `render_template()` |
| `src/pm_agent/templates/plan.md` | 新项目的计划模板 | 同上 |
| `src/pm_agent/templates/tasks.md` | 新项目的任务模板 | 同上 |
| `pyproject.toml` | 打包配置；**新增包数据要在这里登记** | setuptools |
| `evals/specify/case-001.yaml` | 金样本的**输入**（项目名、目标、学习目标） | `tests/test_evals.py` |
| `evals/specify/case-001.recorded.md` | 金样本的**录制**：一份真实模型输出 | `tests/test_evals.py` 回放 |
| `evals/README.md` | 金样本怎么录、什么时候该重做 | 人 |
| `evals/tasks/case-001.spec.md` | 任务拆解金样本的**输入**（一份含三条需求的规范） | `tests/test_evals.py` |
| `evals/tasks/case-001.recorded.md` | 任务拆解金样本的**录制**：真实模型返回的任务清单 | `tests/test_evals.py` 回放 |
| `spec.md` / `plan.md` / `tasks.md` / `项目框架.md` | 本仓库作为"项目工作区"的自身文档 | 人 |
| `src/pm_agent/prompts/specify.system.md` | specify 阶段的**规则**（不含占位符） | `specify.draft_spec()` 作为 system 消息 |
| `src/pm_agent/prompts/specify.user.md` | specify 阶段的**数据模板**（含 `{{name}}` 等占位符） | `specify.draft_spec()` 作为 user 消息 |

---

## 4. 一次命令的完整数据流

静态清单看不出行为，这条链路能。

**`pm-agent stage plan`**

```
cli.stage(name)                        ← Typer 解析参数
  → guarded 装饰器                      ← 异常兜底
    → stages.announce("plan")
      → stages.get_stage("plan")        ← 找不到就抛 PMAgentError（带可选项提示）
        → stages.load_stages()
          → stages._stages()            ← lru_cache，只解析一次
            → importlib.resources.files("pm_agent.stages") 读 stages.yaml
            → yaml.safe_load → 逐字段校验 → Stage(...)
      → 拼成三行字符串
    → console.print(Text(...))          ← Text 防止 Rich 吃掉方括号
```

**`pm-agent init .`**

```
cli.init(...)
  → workspace.create_project(...)
    → format.new_project_meta(...) → yaml.safe_dump 写 project.yaml
    → templates.render_template() → 写 spec.md / plan.md / tasks.md（已存在则跳过）
    → 创建六个数据目录
    → store.ensure_git_repo() → subprocess 调 git init
    → Project.open() → format.check_workspace() 自检
  → 打印新建/跳过清单与 git 结果
```

**`pm-agent ask "..."`**

```
cli.ask(...)
  → model.get_provider(name)           ← 默认 deepseek
    → openai_compat.OpenAICompatProvider.from_env()   ← 预设 + 环境变量
  → provider.complete([Message(...)])
    → urllib 发 POST /chat/completions
    → _extract_text() 解析 choices[0].message.content
  → console.print(Panel(Text(...)))
```

**写一份交接记录（T007）**

```
new_handoff(project, state=..., next_steps=..., open_questions=..., did=...)
  → list_handoffs(project)[0].stem      ← 自动填上 prev（只看文件名，不解析内容）
  → check_handoff()                      ← 当场校验：status 取值、三要素非空、session 格式
write_handoff(project, handoff)
  → check_handoff()                      ← 再校验一次
  → 已存在同名文件？→ 抛 FormatError（拒绝覆盖）
  → Project.write_text("sessions/<session>.md", render_handoff(handoff))
    → files.render_markdown()            ← frontmatter + 正文

读回：read_latest_handoff(project)
  → list_handoffs(project)               ← 按文件名倒序
  → read_handoff() → parse_handoff()     ← 拆 frontmatter、按 ## 标题分节、校验
                                          （认不出的标题会被报出来，不静默丢弃）
```

**生成一份规范（T011）**

```
cli.specify --provider ...
  → model.get_provider()                 ← 默认 deepseek
  → stages.specify.draft_spec(project, provider)
    → prompts.render("specify.system.md")  ← 规则
    → prompts.render("specify.user.md")    ← 数据（项目名、目标、学习目标、日期）
    → provider.complete([system, user])    ← 这里才真的调模型
    → _clean()                             ← 剥掉模型自加的 frontmatter 与代码围栏
  → stages.specify.prepare_spec_change()
    → check_draft()                        ← 五项关键词、长度、回声输出
    → 不合格 → 抛 FormatError（附模型原文），**不产出 Change**
    → 合格 → compose_spec() 保留原 frontmatter → Project.prepare_write()
  → console.print(change.render())          ← 预览（含 diff）
  → typer.confirm("写入 spec.md？")          ← 确认这一步只在 CLI 里（副作用不外溢）
  → Project.apply(change)                    ← 落盘并留可撤回的快照
```

**写一次改动并撤回（T008）**

```
Project.prepare_write("spec.md", 新文本, reason="...")
  → 读原文件的**字节** → ChangeEntry(before_bytes=..., after=...)
  → change.render()                      ← 预览文本（含 diff 片段，超过 12 行截断）
Project.apply(change)
  → 没有实际变化？→ 直接返回：不写盘、也不留历史
  → 建 history/<时间戳>/
  → 写 before/<原相对路径>（原始字节）+ meta.yaml（说明、逐文件行数统计）
  → 按字节写入目标文件
Project.undo_last()
  → 在 history/ 里找**最近的未撤回**记录（meta 里带 undone 的跳过）
  → existed=true → 把快照字节写回去
  → existed=false → **删掉这个文件**（写入前它并不存在）
  → 在 meta 里记 undone，所以再撤一次会继续往前退
```

---

## 5. 测试文件

| 文件 | 覆盖什么 |
| --- | --- |
| `tests/test_format.py` | 格式校验规则：缺字段、坏日期、非法状态、两级严重度 |
| `tests/test_store.py` | 工作区读写：创建、手工改后读回、越权路径被拒、git 初始化 |
| `tests/test_model.py` | 模型层：回声实现、预设与覆盖、缺密钥报错、返回结构异常 |
| `tests/test_cli.py` | 命令端到端：`--help`、init/show/check、`--no-git`、缺密钥报错、`history`/`undo` |
| `tests/test_changes.py` | 预览与撤回：**准备阶段不碰磁盘**、diff 统计、无变化不写盘也不留历史、**撤回逐字节一致（含 CRLF）**、撤回新建文件=删掉、连续撤回往前退、结构上没有直接写入口 |
| `tests/test_specify.py` | specify 阶段（**用假 provider，不联网**）：提示词带上项目元数据、清理模型自加的 frontmatter/围栏、五项校验、**回声输出被拦下**、**失败时不写文件也不留历史** |
| `tests/test_specify_live.py` | 联网测试：真实调用模型跑完整链路（默认跳过，见文件头的开关说明） |
| `tests/test_stages.py` | 阶段框架：四个 key、字段非空、三行输出、未知 key 提示、无副作用 |
| `tests/test_handoff.py` | 交接记录：空会话合法、首份无 prev、prev 链接、同秒不覆盖、拒绝覆盖、**坏记录不拦写入**、**认不出的标题被报出**、手工编辑可读回、各类校验报错 |
| `tests/test_requirements.py` | 需求条目：解析出条目与分组、**正文里的引用不算条目**、重复编号与格式不合规都报出来、**删掉最大号后不复用**、新增落在最后一条之后、**修改只动一行且其他条目逐字不变**、准备阶段不落盘 |
| `tests/test_review.py` | 评审：待澄清两个来源、**只读取不生成**、需求历史（出现 / 修改 / 无记录）、确认写 frontmatter 且**只动 frontmatter**、重复确认为无变化、评审稿含待确认清单、CLI 三个新命令 |
| `tests/test_evals.py` | 金样本回放：录制输出 → 完整链路 → 断言 SC-001（结构完整、条目非空、frontmatter 保留、通过工作区校验） |
| `tests/test_tasks.py` | 任务拆解：解析全字段（含 `[P]`、末尾句号不混进值）、**依赖悬空/重复/格式不合规都报出来**、覆盖缺口（**`（— / —）` 不算缺口**）、就绪任务按优先级排、**三要素缺一写不进去**、**无证据不能完成**、改字段只动那一处、准备阶段不落盘 |

跑测试：`.\.venv\bin\python.exe -m pytest -q`

---

## 6. 更新检查表

改完代码、提交之前，逐条过一遍：

- [ ] 新增 / 删除 / 改名了类或函数？→ 更新 §2 对应表格
- [ ] 依赖关系变了（多 import 或少 import 了内部模块）？→ 更新 §1
- [ ] 非代码文件（`stages.yaml`、模板）的用途变了？→ 更新 §3
- [ ] §4 的数据流还成立吗？
- [ ] §5 的测试覆盖描述还对吗？
- [ ] 顶部的"更新日期"和"对应进度"改了吗？

如果只想核对有没有漏，可以用 AST 重新扫一遍当前签名再和 §2 对照——手抄容易漏，机器不会。
