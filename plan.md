# 项目管理 Agent —— 实现计划（plan.md）

| 项 | 内容 |
| --- | --- |
| 版本 | v0.1（草稿） |
| 状态 | 待评审 |
| 创建日期 | 2026-09-13 |
| 上游 | [spec.md](./spec.md) v0.5（功能需求规范） |
| 范围声明 | 本文档回答 **how（怎么做）**。技术选型只出现在这里，不出现在 spec.md |

---

## 0. 这份文档怎么用

spec.md 定义"做什么、为什么"，plan.md 定义"用什么方式做出来"。三条约束：

1. **plan 可以推翻 spec 里的假设，但不能悄悄改需求。** 如果需要改需求，回到 spec.md 改，并记录版本。
2. **每个技术决策都要写清代价。** 只写好处不写代价的决策，等于没有决策。
3. **本项目用自己跑自己的流程。** 这份 plan 和配套的 tasks.md，就是用本产品要实现的 SDD 流程手工走一遍的产物——先手工做顺，再让 Agent 接管。

---

## 1. 设计原则（本项目的"宪法"）

这六条决定后续所有取舍。任何与它们冲突的实现方案，要么改方案，要么改原则并记录原因。

| # | 原则 | 它约束了什么 |
| --- | --- | --- |
| 1 | **规范先于实现** | 任何功能动手前先有 spec 条目；任务必须能追溯到需求（FR-003、FR-008） |
| 2 | **数据属于使用者** | 项目数据是人类可读、可手工改、可整体带走的文件，不锁在程序或数据库里（FR-042、FR-043） |
| 3 | **不虚构** | 严格区分"有记录依据的事实"与"Agent 的推测"；没有进展就说没有进展（FR-024、FR-033） |
| 4 | **改动可预览、可撤回** | Agent 不静默改写任何东西；每次写入前给预览，且能撤销（FR-034、FR-035） |
| 5 | **每个阶段可讲解** | 讲不清"为什么这么做"的功能，等于没做完（FR-047 ~ FR-049、US-12 为 P1） |
| 6 | **能力沉淀优先于硬编码** | 能做成能力单元的流程，不写死在代码里（FR-025 ~ FR-029） |

---

## 2. 总体形态

> **一句话**：一个本地命令行 Agent，把"项目"当作一个**人类可读的文件目录**，通过会话循环读写这些文件；每轮会话结束时留下交接记录，下次会话据此接上；每项可复用的做法以"能力单元"形式插拔。

三个关键判断：

- **项目即目录**。不引入数据库。项目 = 一个文件夹，里面是 Markdown + YAML + JSON。这样"数据可读可改"（FR-042）和"可导出可携带"（FR-043）几乎不需要额外实现，同时也天然兼容 git 与手工编辑。
- **自建薄 harness，而不是套框架**。本项目要学的就是会话循环、上下文装配、跨会话交接这三件事，用框架会把这些都藏起来。代价是要自己处理上下文预算与失败重试，见 §9。
- **能力单元对齐 Agent Skills 的开放格式**。用 `SKILL.md` + 元数据的目录结构，三级渐进式加载。这样你做的能力单元是可移植的，也是简历上说得清的东西。

---

## 3. 技术选型与取舍

| 决策 | 选择 | 为什么 | 代价 | 何时可以换 |
| --- | --- | --- | --- | --- |
| 语言 | Python 3.11+ | 生态最全、上手最快；agent 相关示例最多，遇到问题好查 | 性能与类型安全弱于 Go/Rust，学习项目无影响 | 不换 |
| 交互形态 | 命令行（Typer + Rich） | 第一阶段最快跑通全链路；Rich 便于做"阶段说明"这类可读输出（FR-047） | 演示观感不如图形界面 | 演示需求变强时，在 CLI 之上加一层本地界面即可 |
| 数据形态 | Markdown + YAML frontmatter + JSON | 人可读可改（FR-042）、diff 友好、可手工救火 | 需要自己维护格式校验与迁移 | 不换，这是原则 2 的直接体现 |
| 模型接入 | **DeepSeek**（`deepseek-chat`），经 `ModelProvider` 接口接入 | 本项目选定的服务商；它提供 OpenAI 兼容接口，接入成本最低 | 受单一服务商的可用性与配额影响 | 随时；换服务商 = 在 `PRESETS` 里加一行，代码不动 |
| 结构化输出 | 提示约束 + 解析 + 校验失败重试 | 需求条目、任务、交接记录都要求结构稳定 | 需要写校验规则与重试上限 | 若接口支持严格结构化输出，可替换解析层 |
| 版本控制 | git（项目工作区可选初始化） | 交接记录 + 提交历史天然构成"跨会话证据"，也是 FR-018 冲突校验的依据 | 需要处理用户不装 git 的情况 | 保持可选 |
| 测试 | pytest + 金样本评测集 | 把 spec 的验收场景变成可跑用例，直接对应 SC 指标 | 需要维护样例数据 | 一直用 |
| 依赖管理 | 虚拟环境 + requirements（或 uv） | 简单、可复现 | 无 | 不换 |

**明确不引入的东西**（避免过度设计）：

- 不引入数据库与服务端。数据形态已由原则 2 定死。
- 不引入向量检索。能力单元与任务的匹配先用"元数据 + 关键词 + 规则"，规模小，够用；等能力单元超过几十个再考虑。
- 不引入多智能体框架。本项目的主线是单 Agent 的长任务可靠性，不是协作拓扑。

---

## 4. 目录结构

要严格区分两个东西：**工具仓库**（你写的程序）和**项目工作区**（工具管理的对象）。

### 4.1 工具仓库

```
pm-agent/
  src/pm_agent/
    cli.py            # 命令入口：init / open / specify / plan / tasks / work / track / report / skill / export
    harness/          # 会话循环、上下文装配、交接记录、冲突校验（§5.3）
    stages/           # SDD 各阶段的实现：specify / plan / tasks / work（让 AI 执行一条任务）/ track / report / converge
    skills/           # 能力单元的加载、匹配、沉淀、版本
    workspace/        # 项目工作区的读写、格式校验、导出与恢复
    model/            # ModelProvider 接口与实现
    explain/          # 阶段说明、依据记录、讲解模式（§6.7）
  skills/             # 内置能力单元（SKILL.md 格式）
  templates/          # spec / plan / tasks / 交接记录 / 项目元信息的模板
  evals/              # 金样本：把 spec 的验收场景变成可跑用例
  tests/
```

### 4.2 项目工作区（工具生成与维护）

```
<project>/
  .git/               # 版本库，默认初始化（plan §11 决策）
  project.yaml        # 项目元信息 + 学习目标（FR-051）
  spec.md             # 规范：需求条目（FR-002、FR-003）
  plan.md             # 本项目的计划
  tasks.md            # 任务清单（FR-008 ~ FR-013）
  evidence/           # 完成证据（FR-014）
  decisions/          # 决策记录（FR-036）
  sessions/           # 交接记录，一次会话一个文件（FR-016）
  reports/            # 生成的进度汇报（FR-030）
  skills/             # 本项目沉淀的能力单元（FR-025）
  history/            # 版本历史：规范与任务的历史快照（FR-005）
```

---

## 5. 三条技术主线怎么落地

### 5.1 SDD：规范 → 计划 → 任务 → 收敛

**阶段划分与产物**

| 阶段 | 输入 | 产物 | 对应需求 |
| --- | --- | --- | --- |
| 立项 | 一句话目标 | `project.yaml` + `spec.md` 骨架 | FR-001 |
| 规范 | 用户描述 | 结构化规范、需求条目、待澄清清单 | FR-002 ~ FR-007 |
| 计划 | 已确认规范 | `plan.md` | — |
| 任务 | 计划 + 规范 | `tasks.md`，任务与需求条目双向关联 | FR-008 ~ FR-014 |
| 执行 | 任务清单 | 状态、证据、中间结论 | FR-015 ~ FR-020 |
| 收敛 | 规范 + 实际产物 | 缺口报告（未覆盖需求 / 无来源任务） | FR-010 |

**双向追溯**是这一层的核心机制：每个任务持有它服务的需求条目 ID，每条需求条目能列出实现它的任务。追溯关系是所有缺口检查、影响分析（FR-022）、汇报来源标注（FR-031）的公共基础——**先做追溯，其他功能才做得便宜**。

### 5.2 Skill：能力单元的格式、加载与匹配

**格式**（对齐 Agent Skills 的开放约定，一个能力一个目录）

```
skills/<skill-name>/
  SKILL.md          # YAML frontmatter + 正文
  reference/        # 需要时才读的细节文件
  examples/         # 用法样例
```

`SKILL.md` 的元数据字段直接对应 FR-026 与 FR-050：

| 字段 | 对应需求 |
| --- | --- |
| `name`、`description` | 能力标识与一句话说明 |
| `when_to_use` / `when_not_to_use` | 适用场景与不适用场景（FR-026） |
| `inputs` / `outputs` | 输入与输出（FR-026） |
| `why` | 为什么这样做（FR-050） |
| `version` | 版本（FR-029） |

**三级渐进式加载**（这是"Skill"这个概念的关键，也是省上下文的手段）

1. 会话开始时只把**所有能力单元的元数据**放进上下文（名称 + 一句话说明）；
2. 判定相关后，才加载该能力单元的 `SKILL.md` 正文；
3. 正文里引用到的 `reference/` 文件，只在真正需要那一步时才读。

**匹配与建议**：任务创建或状态变更时，用关键词 + 元数据规则算相关性，命中则建议使用（FR-027）。使用者拒绝后记入该能力单元的使用记录，短期不再重复建议（对应 FR-027 的"不再纠缠"与 FR-028 的效果记录）。

### 5.3 Harness：会话循环与跨会话交接

这是本项目最有学习价值、也最容易做砸的一层。一次会话的生命周期固定为五步：

1. **装载**：读取 `project.yaml`、当前项目的最新交接记录、任务状态、相关能力单元元数据。
2. **重建状态**：先输出"上次到哪、下一步建议、未决问题"（FR-017），再接受新指令。**顺序不能反**。
3. **执行**：模型循环调用工具（读文件、写文件、改状态、读能力单元正文）。
4. **落盘**：所有写入走"预览 → 确认 → 写入"（FR-034），并保留一份可撤回的变更记录（FR-035）。
5. **交接**：写入 `sessions/<时间戳>.md`：当前状态、下一步建议、未决问题（FR-016）。

**冲突校验**（FR-018）：交接记录声称的产物若在文件系统中找不到，或产物比记录更新，就停下来提示冲突，要求使用者确认，而不是继续。

**上下文预算**：只装配"当前项目 + 当前阶段相关"的内容；规范过长时按需求条目摘要，全文留在文件里按需读取。

**失败显式化**（FR-037）：模型调用失败、结构化输出校验失败、能力未完成时，一律先说明、再给替代路径，不允许返回一个"看起来完成"的结果。

**三层分工**：不是所有"能力"都该做成给模型调用的工具。分错层的代价是——模型被迫做它不该做的判断，或者程序把确定性逻辑交给模型"记得去做"。

| 层次 | 是什么 | 举例 | 谁触发 |
| --- | --- | --- | --- |
| **模型调用的工具** | 模型需要自主决定"做不做、用什么参数"的动作 | 读文件、写规范、改任务状态、读能力单元正文 | 模型在会话循环里发起 |
| **程序自动执行的检查** | 确定性逻辑，不该依赖模型记得 | 格式校验、覆盖缺口检查（FR-010）、变更影响分析（FR-022）、生成写入预览 | 写操作之后自动跑 |
| **Harness 自己的职责** | 模型不该管、也不该看见 | 装配上下文、写交接记录（FR-016）、冲突校验（FR-018）、控制上下文预算 | 会话循环固定执行 |

**工具清单：按需添加，不要一次设计完。** 判断标准只有一条——**当模型缺一个能力就完不成当前任务时，才加**。理由有两个：每个工具的说明都要占用上下文预算；工具越多，"工具选择"这一环越容易出错（这正是 Function Calling 的第一课：工具定义是给模型做决策用的说明书）。

第一个该写的工具是 `read_file`：只读、零风险，却能把整条链路跑通——能力说明、参数校验、错误处理、结果回喂给模型。后面每个工具都是照它的骨架复制。

**工具设计的两条硬规则**：

1. **工具不向模型抛异常，而是返回模型能读懂的错误文本**，模型才有机会自己纠正；真正的失败再由 harness 汇总成"错误 + 怎么办"给人看（FR-037）。
2. **写类工具必须走"预览 → 确认 → 写入"**（FR-034），并在写入前把原文件存进 `history/`（FR-035）。

**交接记录装什么：引用，而不是复制。**

长任务要跨会话恢复的东西不止"进度"一种，但**不该都塞进交接记录**——一旦复制，就有了两份"事实"，两份就会漂移。按各自的权威位置分三类处理：

| 要恢复的内容 | 谁产出它 | track 怎么处理 |
| --- | --- | --- |
| **任务目标**（要什么、约束、验收标准） | `specify` 阶段，写进 `spec.md` 的需求条目 | **只引用条目编号，不复制正文** |
| **执行进度**（当前步骤、已完成、下一步） | **track 自己** | 产出：任务状态 + 交接记录 |
| **工具结果**（接口返回值、中间产物） | 产物落进项目文件；结论由 track 记 | 中间**结论**写进交接记录（FR-020 说的是"中间结论"，不是原始返回值）；**原始返回值不持久化** |
| **决策依据**（为什么走这条路） | 长期决策归 `decisions/`（FR-036，M9） | 短期理由写进交接记录；长期依据只引用文件 |
| **人工反馈**（审批、补充、例外） | 已确认的内容沉淀进规范或决策记录 | **未决的部分**由 track 产出（FR-016 的"未决问题"）；已定的部分只引用 |

为什么"任务目标"绝不能由 track 复制进来：复制的那一刻就产生了第二份事实，而两份事实必然漂移——这恰恰是"重启后目标漂移"这个失败模式的成因。正确做法是交接记录里只写"当前按 `spec.md` 的 FR-023 推进"，目标本身始终只有 `spec.md` 一个权威版本。FR-003 要求需求条目"可被单独引用"，为的就是这件事；FR-018 的冲突校验也是在守同一条线。

判断标准一句话：**交接记录只写"下一个会话不知道、又必须知道"的东西，其余一律写成指向权威版本的引用。**

---

## 6. 关键机制设计

### 6.1 每次会话给模型看什么

固定为四块，按优先级：① 当前项目元信息与学习目标；② 上次交接记录；③ 与当前阶段相关的规范条目与任务；④ 命中的能力单元元数据。**不把整个项目塞进上下文**，其余内容通过"读文件"工具按需获取。

### 6.2 写入前预览与撤回（FR-034、FR-035）

所有写操作返回一份"将要发生的变更"预览（改了哪个文件的哪些行），确认后才写盘；写入前把原文件存入 `history/`，因此撤回等于恢复上一版。这条机制做得越简单越好。

### 6.3 事实与推测分离（FR-024）

输出层的硬规则：每条断言带来源标记。有文件依据的标"事实 + 来源"，模型推断的标"推测"。汇报（FR-031）与风险预警（FR-021）必须走同一个标记机制，否则会出现"预警里混着猜测"的问题。

### 6.4 证据关联：手动为主、按需读取一次（FR-014）

- 手动：使用者给出证据（文件路径、提交号、说明），系统登记到 `evidence/` 并挂到任务上。
- 按需读取：使用者明确要求时，读取一次外部信息（例如仓库最近提交）作为**参考**，仍需使用者确认才记为证据。
- 明确不做：后台轮询与持续同步。

### 6.5 多项目隔离（FR-038 ~ FR-041）

每个项目一个独立目录，互不引用。跨项目总览（FR-040）通过只读扫描各项目的元信息与状态生成，不产生跨项目写入。**能力单元是唯一允许跨项目共享的对象**（FR-041），且引用不改变归属与历史。

### 6.6 导出、恢复与演示副本（FR-043、FR-044）

导出 = 打包整个项目目录；恢复 = 解包到新位置并校验格式。演示副本 = 从真实项目导出一份，作为独立项目使用。因为项目就是目录，这一条几乎不需要额外代码，只要保证格式自洽。

### 6.7 讲解模式（FR-047 ~ FR-049、FR-051）

- 每个阶段启动时输出固定的三行：**目的 / 输入 / 预期产出**。
- 每次给建议时附带依据来源（需求条目 ID、记录文件、能力单元步骤），对应 FR-048。
- 讲解模式下额外输出"还考虑过什么方案、为什么没选"，对应 FR-049。
- 从第一版就记录依据，**不要事后补**——这是 US-12 被列为 P1 的原因。

---

## 7. 里程碑与用户故事映射

顺序按 spec.md 的优先级：P1 先做，且 P1 内的依赖从上往下。

| 里程碑 | 覆盖故事 | 主要覆盖需求 | 出口标准（能演示什么） |
| --- | --- | --- | --- |
| **M0 骨架** | — | FR-042、FR-001 | 能创建一个项目并手动编辑其文件，程序仍能正确读取 |
| **M1 规范** | US-1（P1） | FR-002 ~ FR-007、FR-034、FR-035 | 一句话目标 → 一份可评审规范，含待澄清清单 |
| **M2 任务** | US-2（P1） | FR-008 ~ FR-014 | 规范 → 任务清单，覆盖缺口为 0 |
| **M3 交接** | US-3（P1） | FR-015 ~ FR-020、FR-037 | 中断后重开，Agent 能说清状态与下一步 |
| **M4 讲解** | US-12（P1） | FR-047 ~ FR-049、FR-051 | 随便挑一个阶段问"为什么"，能给出依据与备选方案 |
| **M5 跟踪** | US-4、US-5（P2） | FR-021 ~ FR-024、FR-030 ~ FR-033 | 生成一份逐条有来源的汇报 + 一张风险视图 |
| **M6 能力单元** | US-6（P2） | FR-025 ~ FR-029、FR-050 | 一个做法被沉淀，并在后续任务里被建议复用 |
| **M7 多项目** | US-10（P2） | FR-038 ~ FR-041 | 两个项目互不污染，能力单元可跨项目复用 |
| **M8 演示** | US-11（P2） | FR-043 ~ FR-046 | 别人在你的机器上自己走通一次完整流程 |
| **M9 变更与决策** | US-7、US-8（P3） | FR-022、FR-036 | 改一条需求，先看到影响面；能查到当初为什么这么决定 |

**P1 做完（M0 ~ M4）就应该是一个能用的产品**，后面的里程碑是把它变得好用、好演示、好复用。

---

## 8. 测试与验收策略

三层，越往下越接近"能不能拿出去讲"：

1. **单元测试**（pytest）：格式校验、ID 生成与追溯、缺口检查、冲突校验、导出恢复的一致性。
2. **金样本评测集**（`evals/`）：把 spec.md 的验收场景逐条变成固定输入 + 期望结果。这是"harness"的第二层含义——**评测支架**，也是简历上最有说服力的一块：**每个用户故事都有可复跑的验收用例**。
3. **端到端演示脚本**：对应 SC-007、SC-008、SC-012，按脚本走一遍全链路，确认能被讲清楚。

验收口径以 spec.md §7 的 13 条成功标准为准，测试不通过就不算完成。

---

## 9. 技术风险与对策

| 风险 | 表现 | 对策 |
| --- | --- | --- |
| 模型输出不稳定 | 规范或任务结构跑偏、JSON 解析失败 | 模板约束 + 解析校验 + 有限次重试 + 失败时明确报错（FR-037） |
| 上下文膨胀 | 会话变慢、关键信息被淹没 | 三级渐进式加载（§5.2）+ 按需读文件（§6.1） |
| 文件格式漂移 | 手工编辑后程序读不动 | 每次读取先校验；提供"修复/迁移"命令；模板集中管理 |
| 自动改写破坏数据 | 一次误操作丢内容 | 预览 + 撤回（§6.2）+ git（可选但推荐） |
| 交接记录与实际不符 | 第二天按错误状态继续 | 启动时冲突校验（FR-018），冲突必须由使用者裁决 |
| 演示环境依赖 | 到别人机器上跑不起来 | 项目即目录 + 导出副本 + 引导式首次使用（FR-043 ~ FR-046） |
| 范围失控 | 越写越大，P1 做不完 | 以 M0 ~ M4 为硬边界；P2/P3 的功能不进 P1 里程碑 |

---

## 10. 本版本在技术层面不做

- 不做服务端、多用户与账号体系（对应 spec §2.2）。
- 不做数据库与向量检索（见 §3）。
- 不做后台持续同步外部系统（FR-014 的边界）。
- 不做真实项目的只读共享视图（改用导出副本，FR-044）。
- 不做图形界面；演示通过命令行完成。
- 不做多智能体协作。

---

## 11. 技术决策

| # | 问题 | 决定 | 落实情况 |
| --- | --- | --- | --- |
| 1 | 用哪家模型 | **DeepSeek**（`deepseek-chat`） | 已落地：`model/openai_compat.py` 的 `PRESETS`，默认服务商 `deepseek` |
| 2 | 项目工作区是否默认初始化 git | **默认开** | 已落地：`create_project(init_git=True)`，可用 `--no-git` 关闭 |
| 3 | 能力单元匹配是否加中文分词 | **暂缓**，M6 前用真实样例实测再定（见下） | M6 前无影响 |
| 4 | 导出格式 | **目录复制**，不用压缩包 | T060 按此实现 |
| 5 | 让 AI 执行任务时，产出落在哪里 | **直接写进项目工作区的文件**（相对路径；程序自己维护的文件与 `history/` 等状态目录除外），同一份变更里再留一条记录；任务状态不自动改，完成由使用者确认 | T078 ~ T082 按此实现 |

第 5 条的取舍值得写下来——它**改过一次**，两次的理由都记在这里。让 AI "执行任务"有三种做法：

1. **产出交付物草稿，落到 `evidence/`**（v0.7 初版选的）：最安全，写错了只影响一份证据文件；代价是留一步手工作业，
   使用者得把草稿里的东西自己搬到位。使用者随后指出：这不是他想要的——代码就该落在代码文件里（v0.8 修订）。
2. **直接写进项目工作区的文件**（v0.8 起采用）：code 落代码文件、文档落文档文件，一步到位。风险用三道闸兜住——
   **同一条 `Change`**（预览 + 快照 + 可整体撤回）、**路径限制在项目目录内**、**受保护文件不许碰**
   （`project.yaml` / `spec.md` / `plan.md` / `tasks.md` 归各自的命令管，`history/` 是撤回的地基，写进去会毁状态机）。
3. **调外部编码 Agent 去真正实现**（在别的仓库里跑代码、跑测试）：仍不做。那超出本版本边界，
   也不是这个工具能保证质量的事；本版本保证的是"产出落到位、依据可追溯、改错能退回"。

修订后仍然坚持的一条：**任务状态不由这条命令改**。写入的是文件，"完成"仍要人看过、给出证据（FR-014）。

第 1 条的收益值得记一笔：DeepSeek 提供 OpenAI 兼容接口，所以"换服务商"
最终只是给 `PRESETS` 加一行，会话循环、上下文装配、上层所有逻辑一行没改。
这正是 plan §3 把模型接入收在接口后面的目的——**抽象的成本在写那一层时付，
收益在换供应商时收回**。

### 关于第 3 条：什么是"简单分词"，加了有什么用

**什么是分词**：把一段中文按词切开，得到像 `["需求", "评审", "流程"]`
这样的词表。中文不像英文有空格分隔——英文 `requirement review` 直接
`split()` 就是词，中文"需求评审流程"必须先切。

- 最省事的做法是**按字切**（单字或相邻两字的组合，即 n-gram），不引依赖，几十行代码；
- 更准的做法是接一个中文分词库（如 jieba），代价是多一个依赖；
- 英文场景通常不需要这一步。

**它在这里的作用**：能力单元的匹配（FR-027 要求"任务与能力单元的适用场景
匹配时主动建议使用"）。匹配的本质是算"这个任务的描述"和"这个能力单元声明的
适用场景"像不像。

- **不做分词**：只能整串包含判断。任务写"帮我做一次需求评审"、能力单元写
  "需求评审"能命中；但任务写"评审一下这些需求"就会漏掉。
- **做分词**：两边都切出共同词（需求、评审），命中率明显提高；代价是引入依赖，
  还要防"命中太多、乱建议"——FR-027 明确要求可拒绝且不再纠缠。

**为什么暂缓**：本项目的能力单元数量是个位数，先量再定比提前上库划算。
M6 的实测方案：用**不引依赖的字符 n-gram 匹配**跑一遍，拿你项目里真实的
3 ~ 5 条任务描述测召回；只有漏得明显时才引入 jieba。这样决策有数据，
不是凭感觉。

---

## 12. 与 spec.md 的偏差记录

实现过程中若发现 spec 需要改，**先改 spec.md 并升版本**，再回到这里登记。

| 日期 | spec 变更 | 原因 | 影响的实现 |
| --- | --- | --- | --- |
| 2026-09-13 | 无（实现细化） | 模板放在 `src/pm_agent/templates/` 而不是仓库根 `templates/`，作为包数据随程序分发，避免安装后找不到 | T003 的产物位置；仓库根不再有 `templates/` |
| 2026-09-13 | 无（实现细化） | CLI 增加 `check`（含 `--fix`）与 `show`：格式校验与项目概览是 M0 验证所必需，也是后续阶段的入口 | plan §4.1 的 cli.py 命令清单 |
| 2026-09-22 | 无（实现细化） | T008 的预览与撤回需要一个能看见的出口，CLI 增加 `history` 与 `undo` 两个命令 | plan §4.1 的 cli.py 命令清单；FR-035 的 UX 落点 |
| 2026-09-23 | 无（细化 plan §4.1 的组织方式） | `stages/` 下新增 `specify.py`，它要调模型、要写文件，依赖 `model` 与 `workspace`。plan 原本把"阶段说明"和"阶段实现"笼统放在 `stages/`，但那样会误伤"阶段说明只依赖 errors"这条性质 | 约束收敛为 `stages/__init__.py` 只依赖 `errors`；阶段实现分文件放，`import pm_agent.stages` 仍不会拉进模型层 |
| 2026-09-23 | 无（新增一处格式约定） | T012 约定"编号只增不复用"，因此需要一条**编号水位线**。它放在 `spec.md` 的 frontmatter（`next_requirement`），与 status/version 同属"程序维护的元数据、正文归模型产出"的分工 | 新增 frontmatter 字段；`spec.md` 无 frontmatter 时会被补上一个 |
| 2026-09-23 | 无（CLI 命名细化） | plan §4.1 的路线图里 `tasks` 是"生成任务清单"那个命令；实际实现里 `tasks` 用来**查看**（带 `--ready` / `--coverage` 两个视图），生成拆给 `breakdown`。理由：查看是高频动作，短名字留给高频动作 | plan §4.1 的 cli.py 命令清单 |
| 2026-09-24 | **新增 FR-052**（清空/重置只作用于系统自建的示范项目；失败要报已删内容） | T072 走查的真实事故：`demo --reset` 没给路径 → 落在工具自己的仓库根目录 → `shutil.rmtree` 真的开始删（清掉 `.git/hooks` 与 `.git/logs`，被权限拦住才停）。原实现只有一道"不是文件系统根"的闸，等于允许清掉任意目录 | `transfer.build_demo` 用 `DEMO_MARKER` 标记文件当唯一凭据；新增 `_clear_demo` 与 `_remove_tree`（逐项删除 + 失败时报出已删项数）；`demo` 的 `--reset` 帮助文案与拒绝提示同步改写 |
| 2026-09-24 | **新增 US-13 与 FR-053**（让 AI 执行任务清单里的一条：装配上下文 → 产出交付物 → 留成证据；完成仍由使用者确认） | 使用者指出这是最核心的需求：只有规范与拆解，链路停在文档层。新增需求而非改造旧命令，是为了不破坏既有语义（`track` 仍然是"记一条进展"） | 新增 `stages/work.py` 与 `prompts/work.system.md` / `work.user.md`；CLI 新增 `work` 命令；产出落 `evidence/`（plan §11 第 5 条的三个备选里选了最可退的一条） |
| 2026-09-24 | **修订 FR-053 的产出形态**：从"只留一份 `evidence/` 草稿"改为"**直接写进项目工作区的文件**"，并把受保护文件与路径限制写进需求 | 使用者明确要求"代码应当直接写入工作区的项目文件中"；只留草稿等于把搬运动作留给使用者，而这正是他想让 Agent 接掉的一段 | `work.prepare_work_change` 改成多文件变更：交付物里的文件块 + 一条记录走**同一条 `Change`**（可整体撤回）；新增 `parse_deliverables` 与 `check_artifacts`（路径限制 + 受保护文件 + 重复/空内容）；`changes.prepare_writes` 提供多文件变更 |
| 2026-09-24 | **新增 FR-054 / SC-015**（待澄清的结论回填进规范） | FR-004 只要求"标出并列出问题"，"回答之后往哪写"一直空着——问题会永远挂在未决清单里。使用者要求补上这个闭环 | 新增 `review.resolve_clarification`：**行级手术**只改那一行；条目里的 `[待澄清] 问题` 换成 `**结论**：…`，问题清单里的 `- [待澄清] …` 去掉标记并追加结论（**原文保留**，看得出问过什么）；若该条目此前被确认过，同一次变更里把它的确认作废（内容变了，确认也随之失效）；CLI 新增 `clarify` |
| 2026-09-25 | **新增 FR-055 / SC-016**（同一条任务重复执行要拦） | 真实事故：一条"初始化项目结构"的任务被连跑 6 次，每次都另起一套文件名（`docs/T001-确认记录.md` → `docs/T001-需求评审记录.md` → … → `css/style.css` + `js/main.js` → `styles.css` + `script.js`），项目里并存多份互相冲突的实现，`index.html` 的引用被反复改向。**命令每次都"成功"，错在没人知道这是第几次** | `work.past_deliveries` 从 `evidence/<任务号>-*.md` 读回历次产出的文件清单；`pick_task` 之后由 `work` 检查：有既往产出就**默认拒绝**并列出清单与时间，除非 `--redo`；`--redo` 时把清单带进提示词（`{{previous_files}}`）要求沿用同一套文件；`work` / `track` 的回显加上"项目：<名称>"，因为它俩的项目目录是 `--path`，抄漏会打到别的项目上 |
| 2026-09-25 | **新增 FR-056 / SC-017**（产出后推进状态 + 拒绝时必须给下一步命令） | 三条各自正确的规则叠在一起把人卡死：`work` 有意不改状态（FR-014 的边界）＋ 重复执行要拦（FR-055）＋ 依赖未完成不许动手（FR-012）＝ **T001 有产出却仍是"未开始"，于是既不能重跑它、也不能开始 T002**。问题不在任何单条规则，而在拒绝时没告诉人怎么走 | 新增 `changes.merge`（把多条变更并成一条，仍可整体撤回）；`work.prepare_work_change` 在任务仍是"未开始"时把 `set_task_status(…, "进行中")` 并进**同一条变更**；`pick_task` 的依赖错误信息与 `work` 的重复执行拒绝信息都补上"那条其实已经有产出"与可直接复制的 `track` 命令 |

---

**版本记录**

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v0.1 | 2026-09-13 | 初稿：确定六条设计原则、技术选型与代价、目录结构、三条主线的落地方式、10 个里程碑与 P1 边界、三层测试策略、7 项技术风险 |
