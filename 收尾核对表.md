# 收尾核对表（T072 / T075 / T076）

这三条要**你本人**跑或写——它们验的正是"别人能不能自己走通"，我跑一万遍也不构成证据。
下面是可以照着走的清单；走的时候**把卡住的地方记下来**，那是最有价值的反馈。

**进度**：T072 ✅ 已完成（2026-09-24，四次卡点全部转成了改动）· T076 ✅ 初稿已成（见 [简历材料.md](./简历材料.md)，**等你读一遍改成你的话**）· T075 ⬜ 唯一剩下的：要找一个真人

---

## T072 · 独立走查：不看文档走完 P1 全链路 ✅ 已完成（2026-09-24）

规则：**不许翻 README、不许问我**。卡住就记下来，别硬撑。

下面这一整段可以直接复制到 PowerShell 里逐段执行。**唯一允许看的文档是 `guide`**。

```powershell
# ---- 0) 万一忘了程序在哪：它不在仓库根目录，在 .venv\bin 里
$pm = "C:\Users\asus\Desktop\AI agent\.venv\bin\pm-agent.exe"

# ---- 1) 允许看的唯一一份文档（给没受过训练的人准备的入口）
& $pm guide

# ---- 2) 开一个走查专用的目录，所有东西都放在它里面。
#         千万别在仓库里敲（也不要给 demo 省略目录）
$walk = Join-Path $env:USERPROFILE "Desktop\走查-T072"
New-Item -ItemType Directory -Force $walk | Out-Null
cd $walk

# ---- 3) 建项目：先建一个练手的示范项目，再建一个自己的空项目
& $pm demo 演示项目
& $pm init 我的项目 --name "我的项目" --goal "一句话说清这个项目要达成什么" --learning-goal "我想学到什么"

# ---- 4) 看一眼现状：说清"上次到哪、现在该干嘛"了吗
& $pm show 我的项目
& $pm resume 演示项目

# ---- 5) 讲清"为什么"：目的 / 输入 / 产出 / 依据，再看取舍与备选（这条不需要项目目录）
& $pm stage specify --explain
& $pm stage track --explain

# ---- 6) 主链路。这几条要模型：先设密钥，报错就加 --provider echo 走离线
$env:PM_AGENT_MODEL_API_KEY = "sk-你的密钥"
# 这台机器的 Python 是 MSYS2 版，**必须**设 CA 包，否则 ask 报
# [SSL: CERTIFICATE_VERIFY_FAILED]（T072 走查第二遍就卡在这里）：
$env:PM_AGENT_CA_BUNDLE = "C:\msys64\etc\pki\ca-trust\extracted\pem\tls-ca-bundle.pem"
& $pm ask "在吗"
& $pm specify 演示项目             # 预览里能不能看出改了什么
& $pm breakdown 演示项目           # 覆盖检查有没有说清缺口
& $pm tasks 演示项目 --ready       # 现在能动手的是哪些
& $pm track "T003 做完了，提交 abc123" --path 演示项目      # 会问"对吗？[Y/n]"，敲 y
& $pm report 演示项目 --audience 上级

# ---- 7) 它会主动提醒什么
& $pm risks 演示项目
& $pm tasks 演示项目 --coverage

# ---- 8) 复用一次做法（注意：项目目录要用 -C 给，不是第一个位置）
& $pm skills 演示项目 --suggest "帮我评审一下这份需求规范"
& $pm skill requirement-review --why -C 演示项目

# ---- 9) 收尾：看清"改错了能撤回"
& $pm history 演示项目
& $pm undo 演示项目

# ---- 10) 走完清场：整个走查目录一起删（只删这一个目录）
cd $env:USERPROFILE\Desktop
Remove-Item -Recurse -Force $walk
```

**记什么**：哪一步不知道该敲什么命令、哪个输出看不懂、哪里需要停下来想。

**已经踩到并修好的（2026-09-23，你走查第一遍的记录）**

| 你卡在哪 | 真正的问题 | 现在长什么样 |
| --- | --- | --- |
| 要建项目时找不到对应的指引 | 引导里**没有 `init`**；而且它写的 `pm-agent …` 在仓库里根本敲不通（程序在 `.\.venv\bin\pm-agent.exe`）；参数一个都没解释 | 引导开头加了「**我想做的事 → 敲哪条**」对照表，第 0 步给 `init` 的完整写法并逐项解释 `--name` / `--goal` / `--learning-goal` / `--no-git`，还写明"任何命令后面加 `--help` 就是它的一手说明" |
| 照抄第 5 步报"项目工作区有问题" | `skill` 的项目目录不是第一个位置，要用 `-C` 给；引导里漏了 | 改成 `pm-agent skill requirement-review --why -C ..\演示项目`，并把"哪类命令要用 `--path`"讲清楚了 |
| 没报出来、但顺手发现的 | 新建出来的空项目，**它自己模板注释里那句示例条目**会被当成真条目，`init` 完立刻 `show` 就报"编号不合规" | 注释里的内容一律不算内容：示例、注释掉的旧条目、注释里的待澄清和任务都不算，且行号不偏移 |

可以重走了。这次值得记的是**别的东西**——比如哪句输出读了要停下来想、哪个概念得先解释才看得懂。

### 第二次走查：一次真事故（2026-09-24，已修）

在仓库根目录敲 `pm-agent demo --reset`（**没给路径**）→ 它把当前目录当成重置目标，
`shutil.rmtree` **真的开始删**：

| 事实 | 说明 |
| --- | --- |
| 被删掉的 | `.git/hooks/`（12 个示例钩子）与 `.git/logs/`（reflog）——目录还在，里面空了 |
| 被拦住的 | `.git/objects/` 里的对象文件：权限不足，`rmtree` 在那里抛错停手 |
| 没被碰的 | 提交历史（`git fsck` 干净，本地 `main` 与 `origin/main` 都是 `c91569f`）、整个工作树、`.venv` |

**根因不是手误，是三处设计问题**：`--reset` 只拦"文件系统根"、其余目录照清；
它默认落在当前目录，而 `demo` 这个命令**本意是建项目**；上一级的报错还写着
"要重建就加 `--reset`（会先清空）"——等于在邀请你去清。三处都改了（FR-052、T077）。

**这件事值得记进简历**：走查不是走形式，它当场抓出了一条会毁数据的路径；
而且修复方式不是"加了句提示"，是**把凭据收窄**——只有带 `.pm-agent-demo` 标记
的示范项目才允许被清空。

### 第三次卡点：把必须做的事写成了注释（2026-09-24，已修）

走查走到第 6 步（要模型的那几条）时被证书挡住：`ask` 报
`[SSL: CERTIFICATE_VERIFY_FAILED] self-signed certificate in certificate chain`。
原因不在程序，而在**我给的命令**：这台机器的 Python 是 MSYS2 版，
`PM_AGENT_CA_BUNDLE` 是必设项，我却把它写成了一行注释——照抄的人自然跳过它，
然后必踩这个错。清单里那行现在改成直接执行，并在 `guide` 里补了一句
"报证书错误就设 `PM_AGENT_CA_BUNDLE`"。

值得记的是这个模式：**注释里的"注意事项"等于没说**。要人做的动作，就得是能执行的一行。

### 第四次卡点：分不清"我建的项目"和"演示项目"（2026-09-24，已修）

走完的收尾一问：`init` 时写的是"编写一个简单计算器"，可后面那些命令返回的全是
待办清单的内容——是不是我的输入没生效？

**不是**。数据一直在 `我的项目/project.yaml` 里（`goal: 编写一个简单计算器`、
`learning_goals: [我想学到简单的ui编写]` 都在）。原因是**那串命令的目标全是
`演示项目`**，从头到尾没点过 `我的项目` 的名。这条命令操作哪个项目，完全由你写的
那个目录决定——两个项目的数据互不串门，这正是 FR-038 要求的隔离。

**但这算产品的问题，不算使用者的**：`init` 建出来的是**空**项目（0 条需求），
引导里没把"要自己点名、且建完是空的"讲出来，就会让人以为输入丢了。
`guide` 里已补上这两句。

---

## T075 · 向一个真人完整演示一次

照着 [演示脚本.md](./演示脚本.md) 走，五段：

- [ ] 0:00 说一句它是什么 + 打开目录给人看一眼
- [ ] 1:00 `resume` —— 让人看到"隔一周回来不用重讲背景"
- [ ] 3:00 `stage track --explain` —— 让人看到依据与备选
- [ ] 5:00 `risks` + `report` —— 强调事实/推测分开、每条都有来源
- [ ] 7:00 `skills --suggest` —— 让人看到"换三种说法都认得出"
- [ ] 9:00 `history` + `undo` —— 收在"改错了能撤回"

**压轴**：演示这个仓库自己——`pm-agent resume .`。它管着自己的 76 个任务，
**自举**这件事比任何功能列表都有说服力。

**演示前**：`demo <目录> --reset`，保证从同一个状态出发。

（`--reset` 只对本工具建的示范项目有效——指到别处会被拒绝。见下面 T072 的记录。）

---

## T076 · 简历材料 ✅ 初稿已成（2026-09-24）

定调：**AI 应用方向**。成稿在 [简历材料.md](./简历材料.md)，结构是"简历条目 → 3 分钟讲稿 → 难在哪表 → 面试追问 → 现场可跑的演示证据 → 出处索引 → 短板"。
**你要做的只有一件事**：读一遍，把里面不像你说话的地方改成你自己的话——面试时说别人的句子，会立刻被听出来。

下面是原始素材与起手式，保留备查。

材料要回答三个问题，不是列功能。可以从这几条起手（每条都在这份仓库里有出处）：

**1. 做了什么**

> 用 SDD 流程从零做了一个本地项目管理 Agent：把项目当目录（规范、任务、状态都是人能
> 直接打开的文件），通过会话循环读写；每次会话结束留下交接记录，下次据此接上。

**2. 难在哪、怎么解决**

| 难点 | 做法 | 出处 |
| --- | --- | --- |
| 长任务跨会话接不上 | 交接记录只写引用不抄正文；启动先重建状态、并校验记录与实际是否一致 | `harness/session.py`、`workspace/handoff.py` |
| 模型产出不可信 | 先校验再落盘，不合格就报错并把原文交回；回声实现专门拦掉 | `stages/specify.py` |
| 改动会毁数据 | 所有写入先预览、落盘前存快照、可逐字节撤回 | `workspace/changes.py` |
| 需求编号被复用会让旧引用指错 | 编号水位线，只增不减 | `workspace/requirements.py` |

**3. 能讲清取舍**

`pm-agent design` 导出的设计说明里，每个阶段都有"取舍"和"被放弃的备选"——
直接就是面试时的素材。比如"为什么交接记录不抄规范正文"、"为什么任务清单用
Markdown 行内字段而不是 YAML"。

---

## 已知限制（演示或面试时可能被问到）

- `export` 是**整目录复制**，会把项目目录里的一切都带走——如果项目目录里放着
  虚拟环境（比如本仓库的 `.venv`），也会一起复制。演示前记得先把 `.venv` 挪出去，
  或者只导出 `demo` 建出来的干净项目。
- 三处判断是**规则式**的，不是模型：`track` 的归属判断、`goals` 的目标匹配、
  `skills` 的建议。所以都有"认错"的可能，各自的兜底是：先回显再确认、结果标"推测"、
  拒绝一次安静两周。
- 校验是**关键词级**的（规格、任务、能力单元都是），拦得住漏项与话术，拦不住
  "提到了词却没写好内容"。
