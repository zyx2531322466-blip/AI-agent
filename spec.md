# 项目管理 Agent —— 功能需求规范

| 项 | 内容 |
| --- | --- |
| 文档类型 | 功能规范（Specification） |
| 版本 | v0.5（草稿，待评审） |
| 状态 | 已纳入 2026-09-13 四轮评审结论，见 8.2、8.2.1 与 8.3；**本版无未决问题** |
| 创建日期 | 2026-09-13 |
| 输入 | 初稿："我想做一个项目管理的 Agent，里头用到 SDD、Skill、Harness 等技术，方便我学习上手，写到简历里" |
| 评审补充 | ① 面向**软件项目** ② 试用者需自己动手操作 ③ 项目数据需能直接打开阅读与手工修改 ④ 流程中每一步都能被讲解 ⑤ 需为每个项目单独记录"我想学到什么" ⑥ 需多项目并行 ⑦ 演示与试用的时长仅作参考、不作硬性验收标准 ⑧ 风险需覆盖返工 ⑨ 短期内不会跑第二个真实项目 ⑩ 完成证据以手动关联为主、按需读取为辅 ⑪ 演示用导出副本，不做真实项目的只读视图；离线可用已降级为后续版本考虑，见 8.2.1 |
| 范围声明 | 本文档只回答 **what（要做什么）** 与 **why（为什么值得做）**；不涉及任何技术选型、架构、框架、模型、存储与部署方式 |

---

## 0. 这份文档怎么用

本文档是整个项目的**唯一事实来源（single source of truth）**：先把它评审通过，再进入计划、任务拆解与实现。

- 每条需求都有编号（`FR-xxx`），可被单独引用、讨论与变更。
- 写不清的地方**不猜**，统一用 `[待澄清]` 标出；已拍板的结论记在 8.2，取舍依据记在 8.3，被推翻的结论记在 8.2.1。
- 本版目标是**达成共识**，不是写得漂亮。请在评审时重点质疑第 3 节与第 4 节。

---

## 1. 背景与目标

### 1.1 要解决的问题

项目管理真正消耗心力的不是"画甘特图"，而是四件事：

1. **对齐成本高**：想法停在脑子里，没有变成可评审的规范，团队各说各话。
2. **拆解靠手感**：从"要做什么"到"今天做什么"之间，每次都要手工重做一遍。
3. **状态易丢失**：隔几天回来，忘了上次做到哪、为什么这么决定。
4. **重复劳动**：同一类事情（例如需求评审、设计梳理、周报汇总）反复从头想一遍。

本产品面向**软件项目**（功能开发、重构、工具建设、技术调研等）。这类项目有大量客观证据可用——代码变更、可运行的验证结果、评审结论——所以"做完了"是可以被验证的，而不是只能靠自我申报。反过来，这也要求把证据与需求、任务对应起来，否则证据再多也说明不了进度。

本产品要解决的核心问题：**让"想法 → 规范 → 任务 → 执行 → 汇报"这条链路可以被 Agent 接管一部分，且中途断掉还能接得上。**

### 1.2 使用者

| 角色 | 关心什么 | 在本产品中的主要诉求 |
| --- | --- | --- |
| 项目负责人（主要使用者） | 进度是否真实、决策是否有据 | 快速得到规范、任务与状态全貌 |
| 参与者 / 评审者 | 我该做什么、验收标准是什么 | 可读的任务与需求说明 |
| 上级 / 干系人 | 现在到哪了、有什么风险 | 可核查的进度汇报 |
| 试用者 / 观众 | 这东西到底能干什么、我上不上得了手 | 无需指导即可自行走通一次完整流程，且不接触使用者的真实项目数据 |

本版本不建立账号与权限体系（见 2.2）。"试用者"通过独立环境与示范项目获得体验，而不是通过账号共享。

### 1.3 三条能力主线（为什么这个项目要用 SDD / Skill / Harness）

这三者是本项目的**能力形态**，不是技术选型。它们在产品里各自对应一类必须做对的事情：

| 主线 | 通俗解释 | 在本产品中表现为 | 为什么必须要有 |
| --- | --- | --- | --- |
| **SDD**（规范驱动） | 先把"要做什么"写成可执行的规范，再动手 | 需求以规范条目为单位存在，任务必须能追溯到规范，缺失信息显式标注待澄清 | 没有规范，任务拆解和"做完了吗"都失去判断依据；规范是后续一切变更的锚点 |
| **Skill**（能力包） | 把一类流程性知识打包成可复用、可插拔的能力单元 | 用户可把一次成功做法沉淀为"能力单元"，在同类场景被自动建议复用 | 让 Agent 越用越顺手，而不是每次从零提示；同时把个人经验变成可展示的资产 |
| **Harness**（长任务支架） | 解决"每次新会话都没有记忆"的问题，让长任务能持续推进 | 会话结束时留下交接记录，会话开始时先重建状态，并校验记录与实际是否一致 | 项目管理天然是跨天、跨周的长任务；接不上进度，产品就只是"一次性聊天" |

**其余两条贯穿性要求**（不是独立主线，但决定产品是否可信）：

- **可追溯**：每条输出都能说清它来自哪里（哪条需求、哪个任务、哪次变更）。
- **不虚构**：区分"有记录依据的事实"与"Agent 的推测"，无进展就如实说无进展。

在软件项目场景下，这两条有客观依据可依托：完成与否能落到代码变更与可运行的验证结果上（见 FR-014），预警也能基于实际记录而非印象（见 FR-024）。难点不在于有没有证据，而在于证据与需求、任务之间能否对得上。

### 1.4 项目目标（个人学习与展示视角）

除了做出可用的产品，本项目还承担两个目标，评审时同样算数：

1. **可学习**：全流程能被一个人独立走完，每一步都能讲解"在做什么、为什么需要、依据是什么"，且不需要查阅外部资料；每个项目都能单独记录"我想从中学到什么"，便于事后复盘。
2. **可演示**：他人能在无人指导的情况下自行走通一次完整流程；使用者能讲清 想法 → 规范 → 任务 → 执行 → 汇报 这条链路，以及规范、能力单元、长任务交接三个设计点各自的取舍。

---

## 2. 范围边界

### 2.1 本版本包含

- 多项目并行：多个项目同时存在、互不干扰，能力单元可跨项目复用。
- 单项目全生命周期：立项 → 规范 → 任务 → 执行跟踪 → 汇报。
- 规范、任务、状态、风险、决策、能力单元六类核心对象的创建与演进。
- 跨会话的状态交接与续作。
- 面向不同读者的进度汇报。
- 项目的完整导出与恢复，用于演示准备、备份与转移。
- 供他人演示与试用的路径：可重置的示范项目、无需指导的上手流程。
- 流程可讲解：每个阶段的目的、输入、产出与判断依据都可被说明。

### 2.2 本版本明确不做

- 多人实时协作（同时编辑同一项目、评论、@提醒）。
- 账号体系与权限角色管理（演示与试用改用独立环境 + 示范项目解决）。
- 与外部系统的持续自动同步（双向同步或后台轮询）；只支持在使用者要求时按需读取一次，见 FR-014。
- 真实项目的只读共享视图（改用"导出副本作为演示项目"替代，见 FR-044）。
- 云端托管与跨设备实时同步。
- **离线 / 断网可用**：v0.3 起不再作为本版本要求，留待后续版本考虑（原 US-9 及对应需求已移除，见 8.2.1）。
- 甘特图等重型可视化排期工具。
- 资源成本核算、财务与工时结算。
- 替用户做最终决策（Agent 提供选项与依据，决定权始终在用户）。

---

## 3. 用户故事

> 说明：每个故事都可**独立开发、独立验收**。故事编号在版本之间保持稳定，新增需求一律追加新编号，便于对照评审记录。

| 优先级 | 故事 | 一句话价值 |
| --- | --- | --- |
| P1 | US-1 从一句话想法到可评审的规范 | 让"要做什么"有唯一权威版本 |
| P1 | US-2 把规范拆成可执行的任务 | 把文档变成今天能动手的行动 |
| P1 | US-3 中断之后还能接上 | 跨天跨周不丢状态 |
| P1 | US-12 流程每一步都能被讲解 | 自己能讲清，才写得上简历 |
| P2 | US-4 按需生成进度汇报 | 省掉最高频的重复劳动 |
| P2 | US-5 在拖期之前收到风险提醒 | 从解释过去转为提前预警 |
| P2 | US-6 把重复做法沉淀成可复用能力 | Agent 越用越顺手 |
| P2 | US-10 多项目并行 | 练习、真实、演示项目互不干扰 |
| P2 | US-11 他人自行试用与演示 | 别人能自己跑通，才算真的做出来 |
| P3 | US-7 改需求之前先看到影响 | 减少返工 |
| P3 | US-8 决策可追溯 | 同一件事不被反复推翻 |

**P1 的构成**：US-1 ~ US-3 构成最小可用链路；US-12（可讲解）与之并列，因为它依赖流程中保留的判断依据，事后补做需要重构。

**编号说明**：US-9（断网也能走完流程）已于 v0.3 移除，编号不再复用，原因见 8.2.1。其余编号跨版本保持稳定。

### US-1 从一句话想法到可评审的规范（优先级：P1）

**故事**：作为项目负责人，我想用几句话描述目标，就让 Agent 整理出一份结构化、可评审的项目规范，这样团队不必再花半天对齐"我们到底要做什么"。

**为什么是这个优先级**：规范是整条链路的地基。没有它，后面的任务拆解、进度判断、变更影响全是主观猜测。

**独立验收**：提交一段模糊想法，在一次会话内得到一份含目标、范围、成功标准与待澄清问题的规范，并且可以直接拿去评审。

**验收场景**：

1. **Given** 用户只提供一句话目标，**When** 提交给 Agent，**Then** 返回结构化规范，含目标、范围、使用者、成功标准、待澄清问题。
2. **Given** 输入信息不足以填满规范结构，**When** 生成规范，**Then** 缺失处以显式待澄清标记列出，并给出需要用户回答的具体问题，不自行编造。
3. **Given** 用户提交评审意见，**When** 用户确认采纳，**Then** 规范被更新，且能看出改动前后与改动原因。

---

### US-2 把规范拆成可执行的任务（优先级：P1）

**故事**：作为项目负责人，我想让规范被拆成有优先级、有依赖、能独立完成的任务，这样排期和分工不必每次手工重来。

**为什么是这个优先级**：这是把"文档"变成"行动"的关键一跳。没有拆解，规范只是一份读完就忘的文件。

**独立验收**：给定一份已确认的规范，产出任务清单，每个任务都有可独立验收的完成标准和依赖关系，且能指出覆盖缺口。

**验收场景**：

1. **Given** 一份已确认的规范，**When** 请求拆解，**Then** 每个任务都能关联到具体规范条目。
2. **Given** 任务之间存在依赖，**When** 生成清单，**Then** 依赖被标注且执行顺序合理。
3. **Given** 拆解完成，**When** 展示结果，**Then** 系统指出"未被任何任务覆盖的规范条目"与"找不到来源的任务"。
4. **Given** 用户调整优先级或顺序，**When** 确认，**Then** 计划与依赖关系保持一致，不出现悬空引用。

---

### US-3 中断之后还能接上（优先级：P1）

**故事**：作为项目负责人，我希望隔天或隔周回到项目时，Agent 能自己说清"上次做到哪、下一步做什么、还有什么没定"，这样我不必重新讲一遍背景。

**为什么是这个优先级**：这是"能用"与"用不下去"的分界线。项目管理天然跨天跨周，接不上进度，产品价值就归零。

**独立验收**：在一次会话中途停止，重新开始后 Agent 能重建状态、指出未决问题并给出下一步建议。

**验收场景**：

1. **Given** 上一次会话中途结束，**When** 开启新会话，**Then** Agent 先输出当前状态摘要与建议的下一步。
2. **Given** 上次留下进行中的任务，**When** 新会话开始，**Then** 该任务被标为进行中并保留原有上下文。
3. **Given** 交接记录与实际产物不一致，**When** 系统检测到，**Then** 明确提示冲突并要求确认，而不是静默继续。

---

### US-4 按需生成进度汇报（优先级：P2）

**故事**：作为项目负责人，我想按需得到面向不同读者（自己 / 团队 / 上级）的进度汇报，而不是每次手工整理一遍。

**为什么是这个优先级**：汇报是项目管理中最高频的重复劳动，也最容易掺水，因此对"可核查"的要求最高。

**独立验收**：给定一组状态记录，能生成按 已完成 / 进行中 / 阻塞 / 风险 分组的汇报，每条内容都能指回来源。

**验收场景**：

1. **Given** 任务状态有更新，**When** 请求周报，**Then** 输出按四类分组，且每条标注来源。
2. **Given** 某周期内没有实质进展，**When** 生成汇报，**Then** 如实说明"无进展"及其原因，不生成填充式内容。
3. **Given** 汇报对象从自己切换为上级，**When** 生成，**Then** 详略程度相应调整，结论与风险仍在。

---

### US-5 在拖期之前收到风险提醒（优先级：P2）

**故事**：作为项目负责人，我希望 Agent 主动指出"哪里可能要出问题"，在真正拖期之前给我留出反应时间。

**为什么是这个优先级**：被动汇报只能解释过去；预警才是项目管理真正的杠杆。

**独立验收**：能识别超期、长期无进展、依赖被阻塞、范围蔓延、返工或验收未通过五类情况并给出预警，且预警可被处置、不重复打扰。

**验收场景**：

1. **Given** 某任务超出预期时间仍未完成，**When** 系统检测到，**Then** 生成预警并给出可选应对方案。
2. **Given** 用户对预警标记"已了解 / 无需处理"，**When** 后续再次检测到同一情况，**Then** 不再重复提醒。
3. **Given** 系统中同时存在有依据的风险与 Agent 的推测，**When** 展示预警，**Then** 两类被明确区分标注。
4. **Given** 某任务的经验收判定为未通过、需要返工，**When** 系统检测到，**Then** 生成预警并指出该任务关联的需求条目，供判断是需求不清还是拆分不当。

---

### US-6 把重复做法沉淀成可复用能力（优先级：P2）

**故事**：作为使用者，我希望把一次成功的做法（例如"如何做需求评审"）保存成可复用能力，以后遇到同类场景自动被建议使用，这样 Agent 会越用越顺手。

**为什么是这个优先级**：这是把个人经验转化为可复用资产的机制，也是本项目最值得展示的设计点之一。

**独立验收**：能够把一次成功流程保存为能力单元，并在后续任务匹配时被建议使用，拒绝后不打扰。

**验收场景**：

1. **Given** 一次成功完成的做法，**When** 用户选择沉淀，**Then** 生成一个能力单元，含名称、适用场景、不适用场景、输入、输出、步骤要点。
2. **Given** 已有能力单元，**When** 新任务与其适用场景匹配，**Then** 系统主动建议使用，并允许用户拒绝。
3. **Given** 能力单元被修改，**When** 保存，**Then** 记录版本与修改原因，且历史使用记录不受影响。

---

### US-7 改需求之前先看到影响（优先级：P3）

**故事**：作为项目负责人，我想在改动需求前就看到"会影响什么"，而不是改完才发现要返工。

**为什么是这个优先级**：变更影响分析能显著减少返工，但可以先用人工方式兜住，因此排在 P2 之后。

**独立验收**：给定一条待变更需求，能列出受影响的需求条目、任务、里程碑与已完成的工作量。

**验收场景**：

1. **Given** 一条待变更需求，**When** 请求影响分析，**Then** 列出受影响的需求条目、任务、里程碑与需要重做的验收。
2. **Given** 影响面超过预设阈值，**When** 分析完成，**Then** 明确建议重新评审，而不是建议继续推进。

---

### US-8 决策可追溯（优先级：P3）

**故事**：作为项目参与者，我想知道"当初为什么这么决定"，避免同一件事被反复推翻。

**为什么是这个优先级**：它提升长期协作质量，但不阻塞主链路的可用性。

**独立验收**：能看到任一任务的来源需求，以及相关决策的背景、选项、选择与理由。

**验收场景**：

1. **Given** 一次关键决定，**When** 记录，**Then** 保存背景、可选方案、最终选择与理由。
2. **Given** 查阅某个任务，**When** 追溯，**Then** 能沿任务找到其来源需求与相关决策。

---

### US-10 多项目并行（优先级：P2）

**故事**：作为使用者，我希望同时管理多个项目，并能一眼看到各自的进展与风险，这样练习项目、真实项目和演示项目不会互相干扰。

**为什么是这个优先级**：没有多项目，能力单元的跨项目复用价值就无法体现；但单项目链路可用后产品即可上线，因此排在 P2。

**独立验收**：同时存在 2 个以上项目，各自的规范、任务与状态互不混淆，且能在任一项目内正常作业。

**验收场景**：

1. **Given** 存在多个项目，**When** 查看总览，**Then** 每个项目显示最近进展、阻塞与风险。
2. **Given** 当前处于项目 A，**When** 新建任务，**Then** 该任务只出现在项目 A。
3. **Given** 项目 A 与项目 B 同时存在，**When** 查看或导出其中任一项目，**Then** 结果中不包含另一项目的内容。
4. **Given** 用户切换项目，**When** 进入项目 B，**Then** 界面明确显示当前所处项目，且不残留项目 A 的上下文。

---

### US-11 他人自行试用与演示（优先级：P2）

**故事**：作为使用者，我希望把工具演示给他人，并让对方亲手试一遍，而不必担心我的真实项目被改动或泄露。

**为什么是这个优先级**：演示与试用是本项目的展示目标，但它要在单项目链路可用之后才有意义，因此排在 P2。

**独立验收**：一名未受训的试用者在无指导情况下能独立完成一次完整流程，且使用者的真实项目数据未被读取或修改。

**验收场景**：

1. **Given** 一台干净的环境，**When** 开始演示，**Then** 可在预设的示范项目或真实项目的导出副本上走完完整链路，无需额外准备。
2. **Given** 试用者需要自己动手操作，**When** 其完成试用，**Then** 全程不读取或修改使用者的真实项目数据，且试用留下的内容可被整体丢弃。
3. **Given** 演示中途被打断，**When** 需要重新开始，**Then** 示范项目可回到初始状态。
4. **Given** 一名未受训的试用者，**When** 无人指导地开始使用，**Then** 能独立走完一次完整流程。

---

### US-12 流程每一步都能被讲解（优先级：P1）

**故事**：作为学习者，我希望每个阶段都能说明"在做什么、为什么需要、依据是什么"，这样我能解释自己的设计，而不是只会点按钮。

**为什么是这个优先级**：这是本项目的首要目的。可讲解性依赖流程中保留的判断依据，事后补做需要重构，因此必须在第一版就内建。

**独立验收**：对流程中任一阶段，都能得到该阶段的目的、输入、产出与判断依据，且不需要查阅外部资料。

**验收场景**：

1. **Given** 流程进入某个阶段，**When** 阶段开始，**Then** 系统说明该阶段的目的、输入与预期产出。
2. **Given** 一个输出（例如风险预警或任务拆解结果），**When** 使用者询问依据，**Then** 系统指出对应的需求条目、记录或规则。
3. **Given** 存在多个可选做法，**When** 系统给出建议，**Then** 一并说明取舍与备选方案，而不是只给结论。
4. **Given** 使用者对某个能力单元提出"为什么这么做"，**When** 查看该能力单元，**Then** 得到理由而不只是步骤。
5. **Given** 某个项目记录了"我想学到什么"，**When** 复盘或讲解时查看，**Then** 能对照看到学习目标与已产出内容的对应情况。

---

## 4. 功能需求

### A. 项目与规范

> **为什么需要这一组**：项目管理的混乱往往源自"没有唯一版本的事实"。这一组要求把项目状态收敛到唯一、可引用、可变更的规范上。

- **FR-001** 系统 MUST 允许用户以项目名称加一段自然语言目标创建项目。
- **FR-002** 系统 MUST 把用户输入整理为结构化规范，结构至少包含：目标、范围（含明确不做的部分）、使用者、成功标准、待澄清问题。
- **FR-003** 系统 MUST 以"需求条目"为规范的最小单元，每条均可被单独引用、讨论与变更。
- **FR-004** 系统 MUST 在信息不足时标注待澄清，并列出需要用户回答的具体问题，不得自行填补。
- **FR-005** 系统 MUST 保留规范的版本历史，能够回答某条需求"何时变、为何变"。
- **FR-006** 用户 MUST 能对规范逐条确认或提出修改意见，未确认的条目标记为未确认状态。
- **FR-007** 系统 MUST 支持把规范导出为不依赖本产品即可阅读的文档，供他人评审。

### B. 任务与计划

> **为什么需要这一组**：只有任务能追溯到需求，进度才有意义；只有任务可独立验收，拆解才不是换个说法的许愿。软件项目不缺证据，缺的是证据与任务的对应关系，否则"看起来在推进"很容易掩盖真实的停滞。

- **FR-008** 系统 MUST 支持把规范条目拆解为任务，并维护任务与规范条目之间的双向关联。
- **FR-009** 每个任务 MUST 具备：可独立验收的完成标准、优先级、依赖关系。
- **FR-010** 系统 MUST 在拆解后报告两类缺口：未被任务覆盖的规范条目、找不到来源的任务。
- **FR-011** 系统 MUST 支持里程碑，并允许把任务归入里程碑。
- **FR-012** 系统 MUST 允许用户调整任务的优先级与顺序，且调整后计划与依赖保持一致。
- **FR-013** 系统 MUST 优先排列"可独立交付的最小切片"，使优先级最高的链路能被单独完成并展示价值。
- **FR-014** 每个任务 MUST 能关联可验证的完成证据（例如代码变更、可运行结果、文档或评审结论），且证据由使用者手动关联并确认；系统 MAY 在使用者要求时按需读取一次外部信息作为参考，但 MUST NOT 在后台持续自动同步。仅有状态标记不构成完成。

### C. 执行与状态跟踪

> **为什么需要这一组**：这一组直接对抗"跨会话失忆"。要求交接是显式产物，而不是靠记忆或希望。

- **FR-015** 系统 MUST 维护每个任务的状态（未开始 / 进行中 / 阻塞 / 完成）及状态变更时间。
- **FR-016** 系统 MUST 在每次会话结束时保存交接记录，至少包含：当前状态、下一步建议、未决问题。
- **FR-017** 系统 MUST 在会话开始时先重建状态摘要，再执行用户的新指令。
- **FR-018** 系统 MUST 在交接记录与实际产物不一致时提示冲突并要求确认，不得静默继续。
- **FR-019** 用户 MUST 能用一句话更新任务状态，由系统完成归属判断并回显确认结果。
- **FR-020** 系统 MUST 支持为任务保留中间结论，使该任务可被后续会话继续推进。

### D. 风险与变更

> **为什么需要这一组**：项目管理的价值更多体现在"提前发现"而非"事后解释"；而所有预测都必须与事实严格区分，否则预警会变成噪声。

- **FR-021** 系统 MUST 提供风险视图，至少覆盖五类情况：超期、长期无进展、依赖被阻塞、范围蔓延、返工或验收未通过。
- **FR-022** 系统 MUST 在需求变更被采纳前给出影响分析，覆盖：受影响的需求条目、任务、里程碑与需重做的工作。
- **FR-023** 系统 MUST 允许用户对预警标记处置结果，并据此抑制同类重复提醒。
- **FR-024** 系统 MUST 在输出中区分"有记录依据的事实"与"Agent 的推测"，分别标注。

### E. 知识沉淀与复用

> **为什么需要这一组**：重复劳动是项目管理最大的隐性成本。把做法沉淀为能力单元，产品才会随使用变强。

- **FR-025** 用户 MUST 能把一次成功做法保存为可复用能力单元。
- **FR-026** 每个能力单元 MUST 声明：名称、适用场景、不适用场景、输入、输出、步骤要点。
- **FR-027** 系统 MUST 在任务与能力单元的适用场景匹配时主动建议使用，并允许用户拒绝且不再纠缠。
- **FR-028** 系统 MUST 记录能力单元的采纳情况与使用结果，用于后续判断其有效性。
- **FR-029** 能力单元 MUST 支持独立修订并保留版本，修订不得影响历史使用记录。

### F. 汇报与协作

> **为什么需要这一组**：汇报是外部感知产品质量的唯一窗口，而最容易失信的方式就是写出没有来源的"成果"。

- **FR-030** 系统 MUST 支持按需生成进度汇报，内容按 已完成 / 进行中 / 阻塞 / 风险 分组。
- **FR-031** 汇报中的每条信息 MUST 标注来源（对应哪个任务或哪次变更），不得出现无来源的成果描述。
- **FR-032** 系统 MUST 支持按读者调整汇报详略（自己 / 团队 / 上级），但结论与风险信息必须保留。
- **FR-033** 系统 MUST 在无实质进展时如实说明，不生成填充式内容。

### G. 可信与可控

> **为什么需要这一组**：Agent 会替用户改动项目状态。若改动不可预览、不可撤回、决策无记录，用户很快就会不敢授权；同样地，某项能力没跑成功却不说，使用者会把不完整的结果当成结论。

- **FR-034** 系统 MUST 在任何写入性动作（改需求、改计划、标记完成）执行前，给出用户可读的变更预览。
- **FR-035** 用户 MUST 能撤回最近一次由 Agent 做出的变更。
- **FR-036** 系统 MUST 记录关键决策，包含背景、备选方案、最终选择、理由与时间。
- **FR-037** 系统 MUST 在某项能力未能正常完成时先明确告知使用者并给出替代路径，不得静默降级或伪造结果。

### H. 多项目并行

> **为什么需要这一组**：学习、真实与演示场景会同时存在多个项目，而能力单元的价值恰恰体现在能跨项目复用。这里的原则是**能力共享、数据隔离**。

- **FR-038** 系统 MUST 支持同时管理多个项目，并可在项目之间快速切换，且始终明确显示当前所处项目。
- **FR-039** 系统 MUST 保证项目之间的数据相互隔离：一个项目的需求、任务与记录不得出现在另一个项目中。
- **FR-040** 系统 MUST 提供跨项目总览，至少显示每个项目的最近进展、阻塞与风险。
- **FR-041** 系统 MUST 允许能力单元在项目之间共享复用，且其他项目的引用不改变该能力单元的归属与历史记录。

### I. 数据自主、可携带与演示试用

> **为什么需要这一组**：数据要真正属于使用者——能直接打开看、能手工改、能整个带走；别人要能自己动手跑通，而演示不得以暴露真实项目为代价。

- **FR-042** 项目数据 MUST 采用使用者可直接打开阅读、手工修改的形式，使使用者无需借助本产品即可查看与修改。
- **FR-043** 系统 MUST 允许把一个项目完整导出并在另一环境恢复，用于演示准备、备份与转移。
- **FR-044** 系统 MUST 提供可一键回到初始状态的示范项目；演示亦可使用真实项目的导出副本，用于展示真实复杂度，但副本一经导出即与真实项目相互独立、可被整体丢弃或重置。
- **FR-045** 系统 MUST 为试用者提供可自行操作的空间，其操作不得读取或修改使用者的真实项目数据，且试用留下的内容可被整体丢弃。
- **FR-046** 系统 MUST 提供引导式的首次使用路径，使未受训的试用者可独立完成一次完整流程。

### J. 可讲解（学习模式）

> **为什么需要这一组**：本项目的首要目的是学习。可讲解性必须由流程本身产出，而不是事后补写一份说明文档。

- **FR-047** 系统 MUST 在每个阶段开始时说明该阶段的目的、输入与预期产出。
- **FR-048** 系统 MUST 能对任一输出说明其依据来源（对应哪条需求、哪条记录、哪条规则）。
- **FR-049** 系统 MUST 提供讲解模式：对关键判断说明取舍理由，并列出被放弃的备选方案。
- **FR-050** 每个能力单元 MUST 说明"为什么这样做"，而不仅列出步骤。
- **FR-051** 系统 MUST 允许为每个项目单独记录"这个项目我想学到什么"，并可在阶段推进与复盘时对照查看。

---

## 5. 关键实体（概念层）

> 仅描述"有哪些概念、各自代表什么"，不涉及字段类型与存储方式。

| 实体 | 代表什么 | 与其他实体的关系 |
| --- | --- | --- |
| 项目 | 一段有明确目标与边界的工作 | 包含规范、任务、里程碑、决策与学习目标 |
| 规范 | 项目"要做什么"的唯一权威描述 | 由需求条目组成，可导出供评审 |
| 需求条目 | 一条可单独引用与变更的需求 | 可被多个任务实现；可被变更 |
| 用户故事 | 以使用者视角描述的价值单元 | 归类需求条目，决定优先级 |
| 任务 | 一件可独立完成与验收的工作 | 追溯到需求条目；属于某里程碑；可被阻塞 |
| 产物 / 证据 | 一件任务可验证的交付内容（代码变更、文档、可运行结果等） | 归属某个任务；是判定"完成"的依据 |
| 里程碑 | 一个可对外宣告的阶段成果 | 汇总若干任务 |
| 风险 / 阻塞 | 可能或正在妨碍进展的情况 | 关联任务或需求条目；有处置状态 |
| 决策 | 一次关键选择及其理由 | 关联需求条目或任务 |
| 学习目标 | 一个项目"我想从中学到什么"的记录 | 归属某个项目；在讲解与复盘时对照 |
| 能力单元 | 一套可复用的做法 | 被任务按适用场景引用；有版本 |
| 交接记录 | 一次会话结束时的状态快照 | 关联项目、任务与未决问题 |

---

## 6. 边界与异常情况

- 输入过于模糊、完全无法形成规范时：应只产出澄清问题，不允许生成一份空洞的规范充数。
- 项目长期停滞（数周未触碰）后重启时：状态可能已过期，必须先校验再给下一步建议。
- 同一需求被反复来回改动时：应展示既有决策理由并提示与此前结论冲突，避免无意义摇摆。
- 用户拒绝所有建议时：系统仍须提供价值（例如如实汇报状态），不得靠反复建议刷存在感。
- 任务之间形成循环依赖时：必须被识别并明确提示。
- 用户在会话中推翻此前确认的结论时：应提示与既有规范的冲突并要求确认，而非默默覆盖。
- 记录缺失或无法核实某条信息时：该条内容必须标注为推测或标记为未知。
- 某项能力未能正常完成时：必须先明确告知并给出替代路径，不得静默降级或假装完成。
- 试用者自己动手操作后：留下的内容必须与真实项目隔离，且可被整体丢弃；演示被中断后可回到初始状态。
- 同一任务被反复判定验收未通过时：应提示可能是需求不清或拆分不当，而不是继续重复执行。
- 多项目并行时：风险与汇报必须能按项目区分，避免把 A 项目的问题报进 B 项目。
- 跨项目复用的能力单元被修订时：不得静默改变其他项目已有记录的含义。
- 未指明目标项目时：系统应先确认项目，而不是默认沿用上一次，避免改错项目。
- 交付验收时：以"独立验收"能否完成作为判断标准，不以文档长度或任务数量作为进展证据。

---

## 7. 成功标准（可衡量、与技术无关）

- **SC-001** 从"一句话想法"到"一份可评审规范"，可在一次会话内完成，无需人工补写结构。
- **SC-002** 任务清单对规范条目的覆盖率达到 100%（既无遗漏需求，也无无来源任务）。
- **SC-003** 中断 7 天后重新开始，Agent 能在不追问背景的前提下说清当前状态与下一步。
- **SC-004** 进度汇报中"无来源的成果描述"数量为 0。
- **SC-005** 至少 3 个能力单元被沉淀，其中至少 1 个在后续至少 3 个不同任务中被建议复用，采纳与拒绝均有记录。
- **SC-006** 需求变更的影响分析能列出全部受影响任务，无遗漏。
- **SC-007** 使用者可在不查文档、不求助的情况下独立走完完整流程。
- **SC-008** 向他人演示时，能讲清 想法 → 规范 → 任务 → 执行 → 汇报 的完整链路，以及规范、能力单元、长任务交接三个设计点各自的取舍。
- **SC-009** 同时运行至少 2 个项目，抽查任一项目的需求、任务与记录均不出现在另一项目中。
- **SC-010** 一名未受训的试用者在无人指导下能独立走完一次完整流程，且使用者的真实项目数据未被读取或修改。
- **SC-011** 使用者能逐一讲清每个阶段的目的与理由，每个"为什么"都能在系统内或规范中找到依据，无需查阅外部资料。
- **SC-012** 项目可从一个环境完整导出并在另一环境恢复，恢复后的内容与状态与原环境一致。
- **SC-013** 每个进行中的项目都记录了"我想学到什么"，且复盘时能对照已有产出说明达成情况。

---

## 8. 假设、已确认结论与决策依据

### 8.1 已做出的假设（评审时可推翻）

- 使用者本人是长期使用者；试用者与观众是短期使用者，因此不建立身份与权限体系。
- 项目类型为软件项目（功能开发、重构、工具建设、技术调研等），任务的完成可关联到代码、文档或验证结果。
- 使用环境的网络状况假定基本可用；离线可用不属于本版本要求，见 8.2.1。
- 使用者有明确的学习意图，愿意阅读阶段说明，不追求"零解释直接出结果"。
- 允许 Agent 提出建议并代为整理，但所有写入性改动都必须经用户确认。
- 单机使用为主，不做跨设备实时同步。

### 8.2 已确认结论（2026-09-13 评审）

| # | 确认内容 | 对范围的影响 | 落到哪些需求 |
| --- | --- | --- | --- |
| ① | 面向软件项目 | 完成判定基于可验证证据；难点转为证据与需求、任务的对应关系 | FR-014 |
| ② | 试用者需要自己动手操作 | 需要独立操作空间与数据隔离，试用残留可整体丢弃；仍不引入账号与权限体系 | FR-045、US-11 |
| ③ | 项目数据需可被直接打开阅读、手工修改 | 数据不得锁在本产品内，须能带出门、能手工改 | FR-042 |
| ④ | 需为每个项目单独记录"我想学到什么" | 新增项目级对象"学习目标"，并纳入复盘与讲解 | FR-051、SC-013 |
| ⑤ | 流程每一步可讲解 | 讲解内建为流程的一部分，需保留判断依据与备选方案 | FR-047 ~ FR-050、US-12（P1） |
| ⑥ | 演示与试用的时长不作为硬性验收标准 | 取消原 10 分钟 / 30 分钟时限，改为定性达成 | SC-008、SC-010 |
| ⑦ | 需多项目并行 | 新增项目隔离、快速切换与跨项目总览；能力单元跨项目共享但项目数据不共享 | FR-038 ~ FR-041、US-10 |
| ⑧ | 短期内不会有第二个真实项目 | 原 SC-005 不可观测，改为"同一项目内能力单元被多次复用" | SC-005 |
| ⑨ | 风险视图需覆盖返工 | 风险类型由四类扩为五类 | FR-021 |
| ⑩ | 完成证据以手动关联为主、按需读取为辅 | 明确排除后台持续同步；"完成"的判断权保留在使用者手里，避免系统误判被当成结论 | FR-014、2.2 |
| ⑪ | 演示改用真实项目的导出副本，不做只读视图 | 不引入"同一份数据两种权限"；副本与真实项目相互独立，可整体丢弃或重置 | FR-044、2.2 |

### 8.2.1 已撤销或降级的结论

保留记录，避免同一议题反复讨论：

| 原结论 | 处理 | 影响范围 |
| --- | --- | --- |
| 面向通用知识工作（v0.2 确认） | v0.3 改为面向软件项目 | §1.1、§5 产物定义、FR-014、8.1 相应改写 |
| 必须离线可用（v0.2 确认） | v0.3 降级为**后续版本考虑**，不作为本版本要求 | US-9 已移除；原 FR-041 ~ FR-045 删除（其中"导出与恢复"保留，并入 I 组，v0.4 后编号为 FR-043）；SC-009 删除；2.2 不再排除云端托管 |

### 8.3 关键取舍依据（已定案）

本版没有未决问题。以下两个取舍是 8.2 中 ⑩ 与 ⑪ 的判断依据，保留在此，避免日后重新讨论。

**① 完成证据：手动关联为主、按需读取为辅**（对应 8.2 ⑩）

差别在于**谁来判断任务完成**，以及**误判会以什么形式出现**。

- **手动关联**：使用者告知"任务 X 完成，证据是这次代码变更 / 这份文档"，系统只负责记录与呈现，不主动读取外部系统。
  - 好处：不对接任何外部系统，成本最低，在别人的环境里也能直接演示。
  - 代价：进度真实度取决于记录的勤快程度——漏记，进度就失真。
- **自动获取**：系统主动读取代码仓库、构建或工单信息，据此推断任务是否完成。
  - 好处：不依赖自觉，进度更难被质疑。
  - 代价：需要对接外部系统（与 2.2 冲突）；更麻烦的是误判——"提交了代码"不等于"任务完成"，而这类错误比漏记更隐蔽，因为使用者倾向于相信系统。
- **按需读取**（中间形态）：不做持续同步，只在使用者要求时读取一次外部信息，结论仍由使用者确认。
  - 好处：真实度接近自动获取，成本与风险接近手动关联。

**结论**：以手动关联为主、按需读取为辅。本项目是学习与演示用途，全自动同步的复杂度换不来相应价值；更重要的是，它会把"完成"的判断权从使用者转移到系统，与本产品"不虚构、可追溯"的定位不符。据此把边界写进 FR-014 与 2.2。

**② 演示方式：导出副本，不做只读视图**（对应 8.2 ⑪）

差别在于**演示的说服力**与**是否需要引入权限概念**。

- **不做只读视图**：试用者只能在示范项目里操作，接触不到真实项目。
  - 好处：零风险、零额外机制，与 2.2"不做账号与权限管理"完全一致。
  - 代价：无法展示真实项目的复杂度（真实的规范规模、真实的风险视图），演示说服力弱一些。
- **做只读视图**：可以把真实项目以只读方式展示给试用者。
  - 好处：能展示真实工作成果，说服力更强。
  - 代价：等于引入"同一份数据、两种权限"，是权限系统的雏形，与 2.2 的决定直接冲突。

**结论**：不做只读视图，改用"把真实项目导出一份副本作为演示项目"（FR-043 与 FR-044 的组合）。副本能展示真实内容与真实复杂度，但一经导出即与真实项目相互独立，可被整体丢弃或重置——既拿到说服力，又不必引入权限概念。

---

## 附录 A：术语表

| 术语 | 一句话解释 |
| --- | --- |
| SDD（Spec-Driven Development，规范驱动开发） | 先把"要做什么"写成可执行的规范，再据此做计划、拆任务、实现；规范本身是被持续维护的产物，而非一次性文档。 |
| Skill（能力包） | 把一类流程性知识（步骤、要点、注意事项）打包成可复用、可插拔、可移植的单元，让 Agent 在合适场景自动调用。 |
| Harness（长任务支架） | 解决"每次新会话都从零开始"的问题：用初始化阶段建立环境与记录，用每次会话的交接记录串起长期工作，并要求每轮结束时留下干净、可继续的状态。 |
| 用户故事 | 以使用者视角描述的一条价值单元，用于判断优先级与独立验收。 |
| 最小切片 | 能独立交付并展示价值的最小范围；优先实现它，可以尽早验证方向是否正确。 |
| 待澄清 | 规范中信息不足、需要用户补充的地方；显式标注而不自行假设。 |
| 验收证据 | 用于判定任务完成的客观依据，例如代码变更、可运行的验证结果、文档或评审结论。 |
| 讲解模式 | 系统主动说明每一步的目的、依据与取舍，而不是只给结论。 |
| 示范项目 | 预置的、可一键重置的项目，用于在他人面前演示完整链路。 |

## 附录 B：参考来源

以下资料用于校准 SDD、Skill、Harness 三项实践的主流做法（不构成技术选型建议）：

- GitHub Spec Kit — 规范驱动开发流程与规范模板结构：<https://github.com/github/spec-kit>
- Anthropic — *Equipping agents for the real world with Agent Skills*（2025-10-16）：能力包的构成与渐进式加载设计：<https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills>
- Anthropic — *Effective harnesses for long-running agents*（2025-11-26）：跨会话续作、初始化阶段与交接记录：<https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents>

---

**版本记录**

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v0.1 | 2026-09-13 | 初稿：明确三条能力主线、8 个用户故事、35 条功能需求、8 项成功标准与 6 个待澄清问题 |
| v0.2 | 2026-09-13 | 纳入六项评审结论：新增 US-9 ~ US-12；功能需求由 35 条扩至 52 条并重排编号，新增 H 多项目并行、I 离线可用、J 可讲解、K 演示与试用四组；重写范围边界、成功标准与假设；SC-005 改为不依赖第二个真实项目；新增 6 个待澄清问题，其中"离线时由谁完成语言理解"为可行性关键 |
| v0.3 | 2026-09-13 | 两处范围调整：项目类型由"通用知识工作"改为**软件项目**；"必须离线可用"降级为后续版本考虑。移除 US-9 与原 FR-041 ~ FR-045，功能需求由 52 条收敛至 49 条并重排编号，分组由 A ~ K 收敛为 A ~ J（I 改为"演示与试用"）；删除 SC-009 并重排成功标准至 SC-012；新增 FR-037（能力未正常完成时不得静默降级）；新增"证据是手动关联还是自动获取"这一待澄清问题 |
| v0.4 | 2026-09-13 | 纳入七项新结论：风险视图扩为五类含"返工或验收未通过"（FR-021）；试用明确为"试用者自己动手操作"并允许丢弃试用残留（FR-045）；新增数据可读可改要求（FR-042）与项目级"学习目标"（FR-051、SC-013）；取消 10 分钟 / 30 分钟硬性时限（SC-008、SC-010）；I 组改名为"数据自主、可携带与演示试用"。功能需求 49 → 51 条并重排编号，成功标准 12 → 13 条；8.3 收敛为两个待定问题，并补充选项对比与倾向建议 |
| v0.5 | 2026-09-13 | 最后两个待定问题已定案（8.2 ⑩⑪），**本版无未决问题**：① 完成证据以手动关联为主、按需读取为辅，明确排除后台持续同步，写入 FR-014 与 2.2；② 演示改用真实项目的导出副本、不做只读视图，写入 FR-044 与 2.2。8.3 由"待澄清问题"改写为"关键取舍依据（已定案）"，保留两个取舍的对比与结论以免日后重议。**本次未改动功能需求编号**（仍为 FR-001 ~ FR-051） |
