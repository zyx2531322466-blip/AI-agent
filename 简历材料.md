# 简历材料（AI 应用方向）

> 这份文件是 T076 的产物。**它只负责"怎么讲"**；"做了什么"看 [spec.md](./spec.md)，"怎么设计"看 [plan.md](./plan.md)，"代码长什么样"看 [项目框架.md](./项目框架.md)。
> 每条说法后面都给出了**出处**——面试被追问时，能当场翻出文件和命令，而不是靠回忆。

**一句话（放简历标题下）**

> 用 SDD 从零做了一个**本地优先、可解释、跨会话接得上的 LLM 应用**：把项目当目录（规范、任务、状态都是人能直接打开的文件），用会话循环读写；52 条需求、77 个任务、299 条测试，并且**用它管自己**。

---

## 1. 简历条目（可直接粘贴，按推荐顺序）

**项目管理 Agent**（个人项目，2026.09）　*Python / Typer / Rich / DeepSeek(OpenAI 兼容) / pytest*

- 用**规范驱动开发（SDD）**把 LLM 应用从需求做到验收：53 条功能需求 → 80 个任务 → 14 条成功标准，全链路可追溯（任取一个任务能查到来源需求与相关决策）。
- 实现**"让 AI 执行一条任务"**的核心链路：自动装配该任务的上下文（完成标准、来源需求正文、相关决策、命中的能力单元），**产出的代码/文档直接写进项目工作区**，同一条变更里附一份可追溯记录；四道边界——依赖未满足不执行、产出不合格不写、只写项目内相对路径且不碰程序自己维护的文件、**完成判定仍在使用者手里**。
- 设计**"模型输出不可信"**的兜底链路：模型产出先过结构校验再落盘，不合格就报错并把原文交回；失败路径有专门测试（不写文件、不留历史）；同一链路保留离线回声实现用于跑通与回归。
- 实现模型接入层的**服务商抽象**（一次调用层）：换供应商只改一行预设即可接任意 OpenAI 兼容端点；密钥只从环境变量读、绝不落盘；401 / 缺密钥 / 证书错误分别给可操作提示，**不静默降级**。
- 把**提示词从代码里搬出来做数据**（4 个提示词文件），改文案不碰代码、提示词本身可被人评审；配套 `--explain` 输出每一步的依据、取舍与被放弃的备选。
- 建立 **9 组金样本回放**（固定输入 + 录制真实模型输出），不联网即可重放验证整条链路；加上"必须失败"的路径，**299 条测试通过、1 条联网测试默认跳过**。
- 走查中发现并修复一处**会毁数据的路径**：把示范项目重置的权限凭据从"任意目录"收窄为"只认本工具创建的标记文件"，并让半途失败如实报告已删内容（FR-052 / T077，决策留档在 `decisions/`）。

---

## 2. 三分钟讲稿

**0:00~0:45｜它是什么**

> "我用 SDD 做了一个项目管理 Agent。核心一句话：**项目就是一个人类可读的文件夹**——规范、任务、状态都是你能直接用编辑器打开的文件，Agent 通过会话循环读写它，每轮结束留一份交接记录，下次会话据此接上。
> 我做它不是为了替代 Jira，是为了把'LLM 应用怎么才可信'这件事拆开练一遍：**模型输出不可信怎么办、上下文怎么省、跨会话状态怎么接、改动怎么不毁数据**。"

**0:45~1:30｜怎么做的（体现 SDD）**

> "我先写规范：52 条功能需求、13 条成功标准，再拆成 77 个任务，每个任务都挂到具体需求编号上。所以任何一个任务，我都能反查它是为哪条需求做的、相关决策是什么（`pm-agent trace T077`）。
> 代码也是数据驱动：提示词是 4 个独立文件，阶段说明、能力单元、模板全是内容外置——改文案不碰代码，而且这些内容本身可以被评审。"

**1:30~2:20｜难在哪、怎么解决（挑两个最硬的）**

> "第一个硬点是**模型产出不可信**。我的做法是模型只负责起草，程序负责把关：输出先过结构校验，不合格就报错、把原文交回，**不写盘、不留历史**；同一套链路我留了一个离线的回声实现，专门用来跑通和回归。
> 第二个硬点是**跨会话接不上**。每轮结束我生成交接记录，但**只写引用、不抄正文**——目标写需求编号、决策写文件名。抄正文看着方便，实际是制造第二份事实，两份必然漂移，那正是'重启后目标漂移'的成因。"

**2:20~3:00｜收尾：可信度是设计出来的**

> "最后一点是我最想讲的：这个项目里**所有写入都先预览、落盘前存快照、能逐字节撤回**；风险和建议里凡是推测的都标'推测'，事实标'事实'。
> 最能说明问题的是走查：我照自己的引导走了一遍，在仓库根目录敲了 `demo --reset`，它**真的开始删**工具自己的仓库（清掉了 `.git/hooks` 和 `.git/logs`，被权限拦住才停）。修的时候我没加提示语，而是**把凭据收窄**——只有我自己建的示范项目（带标记文件）才允许被清空。这条从事故到修复的记录，包括当时的备选方案，都在 `decisions/` 里。"

---

## 3. 难在哪、怎么解决（按 AI 应用面试的关注点排序）

| 难点 | 做法 | 出处（可当场打开） |
| --- | --- | --- |
| **模型输出不可信** | 模型只起草、程序把关：关键词级结构校验 → 不合格不落盘；失败时把原文交回并给下一步；回声实现专门拦掉，避免"把回声当结果" | `stages/specify.py`、`stages/tasks.py`、`stages/work.py`、`tests/test_specify.py`、`tests/test_work.py` |
| **让 AI"干活"却不失控** | 产出**直接落进项目文件**，但：文件与记录同属一条变更（一次 `undo` 全退回，连新建目录一起收走）、路径限制在项目内、**保护名单**（`spec.md`/`tasks.md`/`plan.md`/`project.yaml`/`history/`）不许碰、依赖未满足直接拒绝、**同一条任务默认只肯做一次**（有既往产出就拦住并列出它，`--redo` 才放行，且重做时沿用同一套文件）、"完成"始终由人确认后再 `track` | `stages/work.py`、`workspace/changes.py`、`plan.md` §11 第 5 条 / §12、`tests/test_work.py` |
| **"成功"的第二次才是坑** | 重复执行同一条任务时，命令每次都"成功"，但项目里悄悄多出一套互相冲突的文件（真实事故：6 次运行 → 9 个并存文件、入口引用被反复改向）。修法不是加提示，而是让第二次**默认失败**并要求显式 `--redo` | `stages/work.py::past_deliveries`、`spec.md` FR-055、`tests/test_work.py` |
| **三条规则叠在一起会把人卡死** | `work` 不改状态（完成归人）＋ 重复执行要拦 ＋ 依赖未完成不许动手 = 使用者既不能重跑那条、也不能开始后面那条。修法：产出即推进为"进行中"（完成仍由人确认），且**任何拒绝都必须给出可直接执行的下一步命令**——包括"那条其实已经有产出、标完成就能继续" | `spec.md` FR-056、`workspace/changes.py::merge`、`stages/work.py`、`tests/test_work.py` |
| **"写完了"不等于"做完了"，但不必敲两条命令** | 写入之后**当场**问一次"这条任务算完成了吗"（默认 No）；答是则记为完成并把本次产出**自动关联成证据**。`--yes` 只跳过写入确认、**不替人判完成**；`--done` 留给自动化 | `spec.md` FR-057、`cli.py::work`、`tests/test_work.py` |
| **上下文怎么装配** | 一条命令把四样东西读齐：任务本身、来源需求正文（走追溯链）、相关决策、**相关能力单元的正文**（三级加载的第二级，只带命中的、最多两个） | `stages/work.py::gather_context`、`workspace/skills.py` |
| **上下文与成本** | 交接记录**只写引用不抄正文**；能力单元**三级渐进式加载**（先只放元数据进上下文，要用时再读正文、再读引用文件） | `workspace/handoff.py`、`workspace/skills.py`、`plan.md` §5 |
| **跨会话状态** | 会话开始先重建状态摘要、并校验"记录说的"和"实际状态"是否一致（冲突就报出来），每轮结束留交接记录 | `harness/session.py`、`tests/test_session.py` |
| **改动会毁数据** | 所有写入先预览 → 落盘前存快照 → 可逐字节撤回（含 CRLF）；结构上没有绕过 `Change` 的直接写入口 | `workspace/changes.py`、`tests/test_changes.py` |
| **建议的准确性 vs 可解释** | 能力单元匹配不用向量库，用**无依赖的字符 bigram 重叠**：可解释、可离线；代价是会误建议 → 允许拒绝，拒绝后两周内不再提 | `workspace/format.py`（`bigram_overlap`）、`workspace/skills.py`、`plan.md` §11 第 3 条 |
| **评测怎么做** | 9 组**金样本回放**：固定输入 + **录制**的真实模型输出，离线重放整条链路并断言成功标准；另有 1 条联网测试（默认跳过） | `evals/`、`tests/test_evals.py`、`tests/test_specify_live.py` |
| **破坏性操作的边界** | 重置只作用于"带标记文件的自建示范项目"，指向别处一律拒绝；删除逐项进行，半途失败报出已删项数 | `workspace/transfer.py`、`tests/test_transfer.py`、`decisions/2026-09-24-T077-FR-052-破坏性操作只作用于自建的示范项目.md` |
| **需求编号这种"看似小事"** | 编号只增不复用（水位线），否则旧引用会静默指向另一条需求——这类 bug 不报错、最难查 | `workspace/requirements.py`、`tests/test_requirements.py` |
| **"提出问题"容易，"把结论写回去"容易漏** | 待澄清的闭环：`questions` 列出（带序号）→ `clarify` 按序号或来源定位 → **问题原样留着 + 追加结论**，回填走预览/可撤回；若那条需求此前被确认过，确认随之作废（内容变了，"看过了"就不成立了）。多解时**拒绝而不是猜** | `workspace/review.py`、`tests/test_review.py` |

---

## 4. 面试可能追问（附答法要点）

**Q：为什么不用向量库 / embedding？**
能力单元只有个位数，样本量不支持"上库"的判断；我先用无依赖的 bigram 匹配跑真实场景，只有漏得明显时再引入分词或向量检索。这是个**有数据再决策**的取舍，不是不知道有更好的方案（`plan.md` §11 第 3 条写了当时的推理与被放弃的备选）。

**Q：为什么不让模型直接输出 JSON？**
规范首先是**给人评审的文档**。先出 JSON 会让提示词变复杂，而且格式校验失败时更难向使用者解释错在哪。现在是"先出文档 → 再用关键词校验结构"，代价是校验只能到关键词级。

**Q：怎么防止模型胡说？**
三层：提示词里限定只依据输入、不许自己补；产出过结构校验，缺项直接打回；所有写入先给预览、由人确认——**判断权留在人手里**。不确定的地方（比如"这个任务可能和你某个学习目标相关"）一律标"推测"。

**Q：换模型/换服务商麻烦吗？**
只有模型接入层知道服务商是谁，换供应商就是给预设表加一行。DeepSeek 提供 OpenAI 兼容接口，所以 `deepseek` 与 `openai-compat` 共用一份实现（`model/openai_compat.py` 的 `PRESETS`）。

**Q：你怎么测输出质量？**
两层：**金样本回放**（录制真实模型输出，离线断言链路）、**必须失败的路径**（缺密钥、无效密钥、模型返回不完整、回声输出）。录了 9 组，覆盖规范生成、任务拆解、跨会话接上、风险汇报、能力单元匹配、变更影响等。

**Q：为什么是本地文件而不是数据库？**
需求上要求"数据必须能直接打开、手工改、能整个带走"（FR-042）。代价浮现过好几次：格式校验、行级编辑、编号唯一性都得自己写——这也是这个项目最有含金量的部分。

**Q：这个项目和 LangChain 之类的关系？**
没用框架，直接走 OpenAI 兼容接口。因为"上下文装配 + 流程编排"本身就是我要练的东西；代价是错误映射要自己做（401、缺密钥、证书错误各自给人能照做的下一步）。

---

## 5. 现场能跑的演示证据（每条 10 秒内出结果）

```powershell
$pm = "C:\Users\asus\Desktop\AI agent\.venv\bin\pm-agent.exe"
cd "C:\Users\asus\Desktop\AI agent"

# ① 自举：这个仓库自己就是一个被管理的项目
& $pm resume .

# ② 每个阶段都讲依据、取舍、被放弃的备选
& $pm stage specify --explain

# ③ 一个任务 → 来源需求 → 相关决策，一条链查下来
& $pm trace T077

# ④ 改一条需求之前，先看波及面（含连带影响与"要重做的工作"）
& $pm impact FR-021

# ⑤ 风险视图：事实与推测分开标注
& $pm risks .

# ⑥ 能力单元：换三种说法都认得出同一个做法
& $pm skills . --suggest "帮我评审一下这份需求规范"
```

**期望看到什么**：① 会指出"上次到哪、现在能动手的任务"；③ 会列出 FR-052 与那份决策记录；④ 会算出直接相关任务、受影响里程碑，并因"已完成的工作要重做"建议先重新评审；⑤ 目前是"没有风险"（如实说没有，而不是编一条）。

---

## 6. 出处索引（每条说法都能查到）

| 说法 | 出处 |
| --- | --- |
| 52 条需求 / 13 条成功标准 | `spec.md`（v0.6） |
| 77 个任务，只增不删的编号与里程碑 | `tasks.md` |
| 35 个命令 | `pm-agent --help`；`tests/test_cli.py` 会核对文档里写的命令必须真实存在 |
| 299 条通过 + 1 条联网测试默认跳过（共 300 条收集） | `tests/`（`pytest -q`） |
| 源码约 5500 行、测试约 2900 行 | `src/`、`tests/` |
| 提示词外置（4 个文件） | `src/pm_agent/prompts/` |
| 三个内置能力单元与三级加载 | `src/pm_agent/skills/`、`workspace/skills.py` |
| 9 组金样本 | `evals/`（specify / tasks / resume / explain / report / skills / projects / impact / demo） |
| 走查的四次卡点 | `收尾核对表.md`（T072 一节） |
| 关键决策留档 | `decisions/` |

---

## 7. 我自己知道的短板（被问到别硬撑，直接承认）

- **校验是关键词级的**：能拦住漏项和话术，拦不住"提到了词却没写好内容"。
- **建议匹配是规则式的**：`track` 的归属判断、`goals` 的目标对照、`skills` 的建议都是规则=可解释，但会认错，各自的兜底是先回显再确认、标"推测"、拒绝后冷却。
- **短期不会跑第二个真实项目**：所以成功标准从"跨项目复用"改成了"同一项目内多次复用"（`spec.md` 2.2 ⑧）。
- **回填只认"那一行"**：`clarify` 是行级手术，不重新渲染整份规范；原话里若还留着"还没定"这类字眼，它只提示、不代改措辞——措辞是人的事。
- **不做离线可用**：这一条在早期评审里被明确降级为后续版本考虑（`spec.md` 8.2.1）。
