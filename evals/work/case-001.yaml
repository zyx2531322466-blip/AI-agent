# 金样本的输入：一条任务 + 它的来源需求（决策与能力单元当时都没有命中）。
# 对应的录制输出在同目录的 case-001.recorded.md。
#
# 录制方式：2026-09-24 用真实 DeepSeek（deepseek-chat）跑 pm-agent work T001，
# 把产出的正文（去掉程序写的 frontmatter）原样存下来。
name: 待办清单
goal: 做一个只在本地跑的待办清单：能记待办、标完成、按天回顾
learning_goals:
  - 走一遍从规范到执行的完整链路
task_id: T001
spec: |
  ---
  status: draft
  version: 1
  ---

  # 待办清单 —— 功能需求规范

  ## 4. 功能需求

  - **FR-001** 能新增一条待办，字段含标题与是否完成。
  - **FR-002** 能标记待办为完成。
tasks: |
  # 待办清单 —— 任务清单

  ## M1 · 记录

  - [ ] **T001**（— / FR-001）把待办的存储与新增做出来。**完成标准**：新增一条后能读回，空标题被拒绝。**优先级**：P1。**依赖**：无。**状态**：未开始。
