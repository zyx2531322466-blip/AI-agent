## M1 · 基础骨架

- [ ] **T001**（— / —）初始化项目结构与版本控制，建立可运行的空工程。**完成标准**：仓库根目录存在可执行入口与 README，运行后无报错退出。**优先级**：P1。**依赖**：无。

## M2 · 记录待办

- [ ] **T002**（— / FR-001）定义待办数据模型，包含标题与是否完成两个字段。**完成标准**：模型定义落地在代码中，能创建实例并读写这两个字段，有对应单元测试通过。**优先级**：P1。**依赖**：T001。
- [ ] **T003**（— / FR-001）实现新增待办的录入入口与保存逻辑。**完成标准**：输入标题并提交后，新增一条待办被持久化，重新读取时该条存在且完成状态为未完成。**优先级**：P1。**依赖**：T002。
- [ ] **T004**（— / FR-001）实现待办完成状态的标记与取消标记。**完成标准**：对待办执行标记完成后其状态变为已完成，再次执行可回到未完成，状态变更被持久化。**优先级**：P1。**依赖**：T003。

## M3 · 按天回看

- [ ] **T005**（— / FR-002）为待办补充完成时间记录，在标记完成时写入。**完成标准**：任一条待办被标记完成后，其完成时间字段有值且可被读取；未完成的待办该字段为空。**优先级**：P1。**依赖**：T004。
- [ ] **T006**（— / FR-002）实现按日期查询已完成待办的能力。**完成标准**：给定某一天，返回的列表只包含完成时间落在该天的已完成待办，跨天数据不混入；有对应单元测试覆盖边界日期。**优先级**：P1。**依赖**：T005。
- [ ] **T007**（— / FR-002）实现按天回看的展示界面，支持切换日期。**完成标准**：用户选择某一天后，界面列出该天已完成的待办；切换日期后列表随之刷新。**优先级**：P2。**依赖**：T006。

## M4 · 待澄清事项

- [ ] **T008**（— / FR-003）向需求方确认待办标题的最长长度限制。**完成标准**：产出书面结论，明确最大字符数以及超长时的处理方式（截断或拒绝），并记录到需求文档中。**优先级**：P1。**依赖**：无。
- [ ] **T009**（— / FR-003）按 T008 的结论为标题长度加上校验与提示。**完成标准**：输入超过约定长度的标题时，按约定方式处理并给出提示；边界长度可正常保存，有对应测试通过。**优先级**：P2。**依赖**：T008、T003。
