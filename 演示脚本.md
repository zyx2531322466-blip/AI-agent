# 演示脚本（10 分钟版）

给"当着别人的面跑一遍"用的。**时限不是验收标准**——写这个是为了不卡壳，
不是为了掐表。按这个顺序走，每一步都能在前一步的结果上接住。

下面用 `pm-agent` 指代 `.\.venv\bin\pm-agent.exe`（不激活虚拟环境时要写全路径；
仓库根目录没有 `pm-agent` 这个文件）。演示时站在**仓库根目录**跑。

## 准备（演示前，不在计时内）

```
pm-agent demo ..\演示项目 --reset
```

用 `--reset` 保证从同一个状态出发：示范项目的时间戳是固定的，
**重置后与第一次建出来逐字节一致**。演示过一次之后，再跑一遍这条就干净了。

## 0:00 ~ 1:00 先说一句它是什么

> "它把项目当目录：规范、任务、状态都是你能直接打开的文件。
> Agent 每次会话读一遍、干一点、留一份交接记录。"

打开 `..\演示项目` 让人看一眼目录——比说十句都管用。

## 1:00 ~ 3:00 接上：`resume`

```
pm-agent resume ..\演示项目
```

指给人看这三行：**上次做了什么 / 上次留下的结论 / 现在能动手的任务**。

> "隔了一周回来，它不用我重新讲背景。"

## 3:00 ~ 5:00 讲清"为什么"：`stage --explain`

```
pm-agent stage track --explain
```

指给人看"依据"和"被放弃的备选"。

> "每个设计决定都能追到哪条需求、还考虑过什么。"

## 5:00 ~ 7:00 主动发现：`risks` 与 `report`

```
pm-agent risks ..\演示项目
pm-agent report ..\演示项目 --audience 上级
```

重点指两处：风险里**事实与推测分开标**；汇报里**每条都有来源**。

> "上级要的是结论和风险，但它不会编——没有来源的东西写不进去。"

## 7:00 ~ 9:00 越用越顺手：能力单元

```
pm-agent skills ..\演示项目 --suggest "帮我评审一下这份需求规范"
pm-agent skill requirement-review --why -C ..\演示项目
```

> "同一段意思换三种说法它都能认出来；拒绝一次，两周内不会再提。"

## 9:00 ~ 10:00 收尾：安全与边界

```
pm-agent history ..\演示项目
pm-agent undo ..\演示项目
```

> "每次写入前都给预览，改错了能撤回。演示用的项目是独立副本，删掉不影响任何真实项目。"

## 如果被问到"你自己能用它做什么"

就演示这个仓库自己：`pm-agent resume .`——它管着自己的 76 个任务。
**自举**这件事比任何功能列表都有说服力。
