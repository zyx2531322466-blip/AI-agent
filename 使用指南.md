# 使用指南（全流程 + 命令）

> 这份文件回答"**想用这个项目做某件事，该敲什么**"。
> 第一次用请先跑 `pm-agent guide`（那是一份从建项目走到出汇报的走查路线，15 分钟）；
> 这份文件是回头查的手册，按流程顺序排，每条命令都真跑过。
>
> 约定：下面的命令**都假设你已经激活过虚拟环境**（第 0 节，每个新窗口一次），
> 因此在仓库目录里可以直接写 `pm-agent`。

---

## 这个项目能做什么（功能总览）

一句话形态：**一个项目就是一个人类可读的文件夹**——规范、任务、状态都是你能直接用编辑器打开的文件；这个 Agent 通过"会话循环 + 交接记录"读写它，把每次可复用的做法沉淀成能力单元。共 **37 条命令**，下面按能力分组；每条都标了**它会不会改你的项目**（读＝只看，写＝会留下东西，而且**写入前都给预览、事后能 `pm-agent undo` 撤回**）。

### 1. 上手与骨架

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `guide` | 第一次用？把"建项目 → 走完主链路 → 出汇报"整套讲一遍，不用先读文档 | 读 |
| `version` | 显示版本号 | 读 |
| `init` | 建一个项目工作区（project.yaml / spec.md / plan.md / tasks.md + 六个数据目录 + git）；已存在的文件一律跳过 | 写 |
| `demo` | 建一个**自带数据**的示范项目（3 条需求、3 个任务、一份交接记录），`--reset` 回到初始状态 | 写 |
| `show` | 项目概览：名称、目标、学习目标、需求条数、任务完成数 | 读 |
| `check` | 体检工作区格式：问题出在哪、怎么回事、怎么办；`--fix` 补建缺失的数据目录 | 读（`--fix` 写） |

### 2. 把想法变成规范（SDD 第一段）

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `specify` | 调模型把目标整理成结构化规范（目标/使用者/范围/成功标准/待澄清），写进 `spec.md`；产出先校验再落盘，不合格报错并把原文交回 | 写 |
| `requirements` | 列出需求条目（编号 / 分组 / 正文），`--limit` 控制条数 | 读 |
| `questions` | 列出还没答案的 `[待澄清]` 问题（带序号） | 读 |
| `clarify` | **回答一处待澄清并把结论回填进规范**：问题原样留着、后面记上结论；若该需求此前被确认过，确认随之作废 | 写 |
| `confirm` | 逐条确认"看过了、可以照它做"（记在 frontmatter 的 `confirmed` 里） | 写 |
| `review` | 导出一份**不依赖本工具**就能读的评审稿到 `reports/`（含待确认条目与待澄清问题） | 写 |

### 3. 把规范变成任务（SDD 第二段）

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `breakdown` | 调模型按当前规范产出任务清单（每个任务带完成标准、优先级、依赖、来源需求），写进 `tasks.md`；**注意：整份覆盖** | 写 |
| `tasks` | 看任务：全部 / `--ready`（现在能动手的，按优先级）/ `--milestone` / `--coverage`（覆盖缺口） | 读 |
| `impact` | 改一条需求**之前**先看会波及什么：直接相关任务、顺依赖走到底的连带影响、受影响里程碑、可能要重做的已完成工作；超阈值会建议先重新评审 | 读 |
| `trace` | 查一个任务的来龙去脉：来源需求 + 提到它的决策记录 | 读 |

### 4. 干活与状态（SDD 第三段：执行）

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `work` | **让 AI 接手一条任务**：自己装配上下文（完成标准 → 来源需求正文 → 相关决策 → 相关能力单元）→ 产出该交付的文件并**直接写进项目**（附一份记录）→ **当场问一句"这条任务算完成了吗"**，答是就把本次产出自动关联为完成证据 | 写 |
| `track` | 用**一句话**更新任务状态（如 `"T003 做完了，提交 abc123"`）：程序先回显它听懂了什么，再让你确认 | 写 |
| `resume` | **每次开会话的第一件事**：重建状态摘要——上次做到哪、上次留下的结论、现在能动手的是哪些；记录与实际不符会报冲突 | 读 |
| `handoff` | 把这一轮的状态写成交接记录到 `sessions/`（事实部分自动填，**"这轮做了什么"留白给你**） | 写 |
| `history` | 变更记录，最近在前；`--requirement FR-003` 看某条需求的来龙去脉 | 读 |
| `undo` | 撤回最近一次变更，**逐字节**回到写入前（文件与状态一起退，连新建目录也收走） | 写 |

### 5. 风险、汇报与复盘

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `risks` | 五类风险（超期 / 长期无进展 / 依赖被阻塞 / 范围蔓延 / 返工或验收未通过），每条标**事实**还是**推测**；`--ack` 处置后不再重复提醒，`--all` 连已处置的也列 | 读（`--ack` 写） |
| `report` | 进度汇报：四类分组、每条带来源；`--audience 自己/团队/上级` 换读者，`--since` 限周期，`--write` 存进 `reports/` | 读（`--write` 写） |
| `goals` | 对照"这个项目我想学到什么"与已产出的东西（匹配结果标**推测**） | 读 |
| `design` | 导出设计说明：把每个阶段的"为什么这么设计"汇总成一份可读文档 | 写 |

### 6. 能力单元（把做法沉淀下来复用）

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `skills` | 列出能力单元（**只显示元数据**，省上下文）；`--suggest "一段描述"` 按意思找——同一段意思换三种说法都认得出。内置四个：需求评审 / 风险复盘 / 周报汇总 / **完成前验证**（`verification-before-completion`，逐字复刻自 obra/superpowers，MIT） | 读 |
| `skill` | 看一个能力单元：默认正文，`--why` 看"为什么这样做"，`-r` 读第三级引用文件 | 读 |
| `skill-save` | 沉淀一个新能力单元（七要素缺一存不进去），`--update` 改成修订（不动使用历史） | 写 |
| `skill-use` | 记一次采纳或拒绝；**拒绝后两周内不再建议它** | 写 |
| `skill-share` | 把一个项目的能力单元复制到另一个项目（复制，来源不受影响） | 写 |

### 7. 多项目与搬运

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `projects` | 跨项目总览：扫一个目录下的所有项目，各自进展、阻塞与风险（**只读**，扫完文件指纹不变） | 读 |
| `export` | 把项目整个导出到别处（**目录复制**，不打包）；`--for-demo` 导出一份当演示用的独立副本 | 写 |
| `restore` | 从一份导出恢复成项目，并校验它能不能正常打开 | 写 |

### 8. 讲清"为什么"与留痕

| 命令 | 做什么 | 读/写 |
| --- | --- | --- |
| `stage` | 讲一个阶段的来龙去脉：目的 / 输入 / 预期产出 / **依据**（对应哪几条需求）；`--explain` 再给取舍与被放弃的备选 | 读 |
| `decide` | 记录一次关键决定：背景、备选方案、最终选择、理由；**五样少一样都写不进去** | 写 |
| `ask` | 调一次模型，验证接入是否配好（不带项目上下文，不产出项目内容） | 读 |

### 9. 贯穿所有命令的几条性质

- **写入前一律给预览**，落盘前把原内容存进 `history/`，事后 `undo` **逐字节**撤回（连换行风格都一致）。
- **模型产出不可信**：`specify` / `breakdown` / `work` 的产出都先过结构校验，不合格就报错、**不写盘、不留历史**，并把原文交回来给你看；离线回声实现专门被拦掉，不会冒充真产出。
- **数据是你的**：全是 Markdown / YAML / JSON，能直接打开、手工改、整个带走（`export`）。
- **事实与推测分开**：风险视图、学习目标对照里，凡是推断出来的都标"推测"。
- **内容外置**：提示词、阶段说明、模板都是数据文件，改文案不用碰代码，它们本身也能被人评审。
- **规则式判断的地方会明说**：`track` 的归属、`goals` 的匹配、`skills` 的建议都是规则（编号优先 + 字符重叠），所以认不准时会问、会标"推测"、拒绝后会安静两周。

### 10. 它明确不做的事（免得你找半天）

- **不替你判定"完成"**：`work` 写完会问你一句，`track` 要证据——"做完"始终由你点头。
- **不是"一句话从头做到尾"的自主 Agent**：理解需求、拆解任务、执行任务是**分开的三步**，每步都要你确认。没有"输入一句话就直接做完整个项目"这种命令。
- **没有"临时需求"这个概念**：需求一落地就有编号（FR-0xx），编号只增不复用（它是各处引用的锚点）。
- **没有交互式会话/REPL**：每条命令都是"敲一条 → 看预览 → 确认 → 结束"。
- 不做多人实时协作、账号与权限、云端托管与跨设备同步、外部系统的持续自动同步、甘特图类重型排期、工时与成本核算，也不替你做最终决策（给选项与依据）。
- 早期版本明确把"**离线/断网可用**"降级为后续版本考虑（模型调用需要联网；没有密钥时只能走 `--provider echo` 看流程）。

> 想要某个命令的参数细节，看第 12 节的**命令速查表**；想知道"从零到出汇报怎么走"，看第 0~11 节的流程。

---

## 0. 每个新终端窗口的开场（三条）

```powershell
cd "C:\Users\asus\Desktop\AI agent"
& ".\.venv\bin\Activate.ps1"      # 激活虚拟环境；之后直接写 pm-agent 就行
$env:PM_AGENT_MODEL_API_KEY = "sk-你的DeepSeek密钥"
$env:PM_AGENT_CA_BUNDLE = "C:\msys64\etc\pki\ca-trust\extracted\pem\tls-ca-bundle.pem"
```

激活后验证一下接没接上（会真调一次模型）：

```powershell
pm-agent ask "在吗"
```

看到模型回答就成了。**没有密钥也想跑流程**：每条要模型的命令加 `--provider echo`，走离线回声实现——能看清流程走到哪一步，但内容不是真模型产出（这是有意的，免得把回声当成结果）。

### 想省掉每次设置（可选，一次到位）

环境变量只活在**当前窗口**，重开窗口要重设。想一劳永逸，设成用户级变量，之后新窗口自动带上：

```powershell
[Environment]::SetEnvironmentVariable("PM_AGENT_MODEL_API_KEY", "sk-你的DeepSeek密钥", "User")
[Environment]::SetEnvironmentVariable("PM_AGENT_CA_BUNDLE", "C:\msys64\etc\pki\ca-trust\extracted\pem\tls-ca-bundle.pem", "User")
```

### 两个环境上的坑

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| `无法加载文件 …Activate.ps1，因为在此系统上禁止运行脚本` | PowerShell 执行策略 | 执行一次 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`，然后重开窗口 |
| `CERTIFICATE_VERIFY_FAILED … self-signed certificate` | 本机 Python 是 MSYS2 版，没有系统 CA | 设 `PM_AGENT_CA_BUNDLE`（就是上面那条）；本机路径实测有效 |

**忘了激活虚拟环境**也没关系：把 `pm-agent` 换成 `.\.venv\bin\pm-agent.exe` 一样能跑。
只是要记住：仓库根目录里**没有** `pm-agent` 这个文件，所以写路径就得写**全**
（写一半、漏掉 `.venv\bin` 那一段，PowerShell 会告诉你"无法将…识别为 cmdlet"）。

---

## 1. 先看懂它的形态

**一个项目 = 一个目录**。目录里全是你能直接打开、直接改的文件：

```
我的项目/
  project.yaml     项目元信息（名称、目标、学习目标、状态）
  spec.md          规范：需求条目 FR-001、FR-002 …
  plan.md          计划（技术方案）
  tasks.md         任务清单：T001（依赖、优先级、状态、证据）
  sessions/        交接记录：每次会话结束留下"上次到哪、下一步"
  decisions/       关键决定（背景、备选、选择、理由）
  reports/         汇报与评审稿、设计说明
  skills/          能力单元：可复用的做法
  history/         每次写入前的快照（撤回用）
  evidence/        完成证据
```

**每条命令操作哪个项目，完全由你写的那个目录决定**；两个项目之间数据不互通。绝大多数命令的项目目录是**第一个位置参数**（`pm-agent show 我的项目`），少数命令第一个位置被别的东西占了（`track` 是那句话、`skill` 是能力单元的名字、`confirm` 是需求编号），这类命令要用 `--path`（简写 `-C`）给项目目录。

主链路就五个动作：

```
想法 --specify--> 规范 --breakdown--> 任务 --track--> 执行 --report--> 汇报
                    ↑                                     │
                    └──────────── handoff（留交接记录）─────┘
```

---

## 2. 建项目

### 2.1 建自己的项目

```powershell
pm-agent init 我的项目 --name "我的项目" --goal "一句话说清这个项目要达成什么" --learning-goal "我想学到什么"
```

- `我的项目` 是**目录**，不存在会自动建；已存在的文件**一律跳过、绝不覆盖**。
- `--name` 与 `--goal` **必填**；少了会直接报错并告诉你缺哪个。
- `--learning-goal` 可以重复写多次（对应"这个项目我想学到什么"，FR-051）。
- 不想初始化 git 版本库就加 `--no-git`（默认会初始化）。
- **建完是空项目**：元信息 + 空模板 + 数据目录，需求 0 条。内容要靠第 4 节的 `specify`，或者你自己编辑 `spec.md`。

### 2.2 只想练手或者要演示：用示范项目

```powershell
pm-agent demo 演示项目
```

它自带 3 条需求、3 个任务和一份交接记录，所以 `resume`、`risks`、`report` 立刻有东西可看。

```powershell
pm-agent demo 演示项目 --reset      # 回到初始状态（时间戳固定，重置后逐字节一致）
```

`--reset` **只认本工具建的示范项目**（目录里有 `.pm-agent-demo` 标记文件）。指到别处——你的真实项目、仓库、桌面——它会直接拒绝，**一个字节都不动**。真实项目要重建，请自己删那个目录。

### 2.3 看一眼现状 / 体检

```powershell
pm-agent show 我的项目
pm-agent check 我的项目
pm-agent check 我的项目 --fix      # 补建缺失的数据目录
```

`show` 给项目概览（名称、目标、学习目标、需求条数、任务完成数）；`check` 只读体检，格式问题会告诉你"出在哪、怎么回事、怎么办"。

---

## 3. 每次开工：先接上

```powershell
pm-agent resume 我的项目
```

**每次开新会话的第一件事**。它会重建状态摘要并回答三件事：上次做到哪、上次留下的结论、现在能动手的是哪些。如果交接记录和实际状态对不上（比如记录里提到某个任务但清单里没有），它会明确报冲突而不是含糊过去。

第一次建的项目会说"还没有交接记录"，正常。

---

## 4. 规范阶段：把想法变成可评审的需求

```powershell
pm-agent specify 我的项目                          # 调模型起草规范，先给预览，确认后才写入
pm-agent specify 我的项目 --goal "换个角度描述"     # 覆盖项目目标这一轮的输入
pm-agent specify 我的项目 --provider echo          # 没密钥时走离线回声
pm-agent specify 我的项目 --yes                    # 跳过确认（自动化时用）
```

预览会给出 diff（改了哪些行）；**写入前会把原内容存进 `history/`**，反悔就 `undo`。模型产出会先过结构校验（背景目标、范围、待澄清、成功标准等），不合格直接报错并把原文交回，**不写盘、不留历史**。

围绕规范的四条查询与管理命令：

```powershell
pm-agent requirements 我的项目 --limit 20          # 列出需求条目（编号 / 分组 / 正文）
pm-agent questions 我的项目                        # 列出还没答案的 [待澄清]（带序号）
pm-agent clarify "标题最长 80 字，超出拒绝保存" --for FR-003 --path 我的项目   # 回答一处，结论回填进规范
pm-agent confirm FR-002 --path 我的项目            # 逐条确认"看过了，可以照它做"
pm-agent review 我的项目                           # 导出一份给别人读的评审稿到 reports/
```

注意 `confirm` 的项目目录要用 `--path`，`review` 的是位置参数——这条差异是因为 `confirm` 的第一个位置留给了需求编号。

**待澄清的闭环**（FR-054）：`questions` 只负责把问题摆出来，回答要用 `clarify`。回填的写法是"**问题原样留着，后面记上结论**"：

```markdown
- **FR-003** 标题长度上限还没定。 待办标题最长允许多少字？ **结论**：标题最长 80 字，超出拒绝保存。
```

- `--for` 可以给序号（`--for 1`）或来源（`--for FR-003` / `--for 待澄清问题`）；**只剩一条时不用写**，有多条而不写会被拒绝——回填错地方等于给规范写进一条错结论，所以它不猜。
- 回填后这一处不再算未决；`pm-agent questions 我的项目` 会少一条。
- 如果这条需求**此前被确认过**，它的内容变了，确认也随之失效——命令会把那条确认摘掉并提示你重看一遍。
- 回填走正常流程：先给预览、确认后才写，反悔就 `pm-agent undo 我的项目`。

**手工改也完全可以**：`spec.md` 就是普通 Markdown，改完再 `pm-agent check 我的项目` 让程序校一遍。

---

## 5. 任务阶段：把规范拆成能动手的活

```powershell
pm-agent breakdown 我的项目                        # 调模型拆任务，先给覆盖检查与预览
pm-agent breakdown 我的项目 --yes                  # 跳过确认

pm-agent tasks 我的项目                            # 全部任务
pm-agent tasks 我的项目 --ready                    # 现在就能动手的（依赖已满足，按优先级排）
pm-agent tasks 我的项目 --milestone M1             # 只看某个里程碑
pm-agent tasks 我的项目 --coverage                 # 覆盖缺口：哪条需求还没有任务为它服务
```

改一条需求之前，先看会波及什么——这条命令对应"别改完才发现要返工"：

```powershell
pm-agent impact FR-007 --path 我的项目
```

它会列出直接相关的任务、**顺着依赖走到底的连带影响**、受影响的里程碑，以及已经完成、可能得重做的工作；影响面超过阈值就建议先重新评审，而不是让你继续推。

想知道某个任务的来历：

```powershell
pm-agent trace T012 --path 我的项目                 # 来源需求 + 提到它的决策记录
```

---

## 6. 执行阶段：记进展、可反悔

### 6.1 让 AI 直接把一条任务做出来（核心命令）

```powershell
pm-agent work --path 我的项目            # 不指定就挑"现在能动手"里优先级最高的一条
pm-agent work T003 --path 我的项目        # 点名做哪一条
pm-agent work T003 --path 我的项目 --yes  # 跳过"要不要写入"的确认，但**不**替你判完成
pm-agent work T003 --path 我的项目 --yes --done   # 写入并直接记为完成（给自动化用）
pm-agent work T003 --path 我的项目 --provider echo   # 没密钥时看流程（会被拦下，不会冒充产出）
```

它做的事：**自己把上下文读齐**（这条任务的完成标准 → 它的来源需求正文 → 相关决策 → 相关能力单元的正文），
调模型产出**这条任务该交付的文件**，**直接写进项目目录**（代码落代码文件、文档落文档文件），
同一条变更里再留一份记录 `evidence/<任务编号>-<时间>.md`（说明这次产出了哪些文件、依据什么上下文），
并打印"上下文来自哪儿"。

它的边界（每条都对应 FR-053 里的一个"不"）：

- **不碰程序自己维护的文件**：`project.yaml` / `spec.md` / `plan.md` / `tasks.md` 归各自的命令管，
  `history/` 是撤回的地基——模型要是想写这几个地方，直接拒绝并说明；
- **不写出项目外面**：只接受项目内的相对路径（绝对路径、含 `..` 的一律拒绝）；
- **不改任务状态**："完成"仍要你审过之后用 `track` 确认（FR-014：仅有状态标记不构成完成）；
- **不越过依赖**：指定一条依赖未完成的任务会直接拒绝并告诉你缺哪条。

**改错了怎么办**：这一批文件与那份记录属于**同一条变更**，`pm-agent undo 我的项目` 一次全退回
（连它新建的目录也一起收走），不会只退一半。

**同一条任务别跑第二遍**（FR-055）：它会先告诉你"这条任务以前产出过什么"，然后**拒绝**。
因为这条命令写的是真实文件，跑第二遍很容易堆出第二套互相冲突的实现（真实发生过：一条
"初始化项目结构"的任务跑了 6 次，项目里并存 9 个文件、`index.html` 的引用被反复改向）。
两条正路：

- **想重来** → 先把上一次退掉：`pm-agent undo 我的项目`，再跑 `work`；
- **确实要叠加**（比如你想留两版做对比）→ 加 `--redo`，这时它会把上次产出的文件清单带进上下文，
  要求沿用同一套文件名，而不是另起一套。

典型节奏：`work` 写入文件 → 你照产出里第 3 节的办法跑一遍验证 → 没问题就
`track "T003 做完了，提交 abc123" --path 我的项目` 记证据；不对就 `undo`。

### 6.1.1 写完就会问你一句"算完成了吗"

写入之后**不用再敲第二条命令**（FR-057）：

```
把这批文件写进项目？ [Y/n]: y
已写入 index.html、styles.css、script.js、evidence/T001-….md
下一步：照产出里第 3 节的办法，自己跑一遍验证；不对就整体撤回：pm-agent undo 我的项目
这条任务算完成了吗？ [y/N]:
```

- 回答 **y** → 任务立刻记为"完成"，**证据自动挂上**（指向这次的记录文件与交付的文件名，例如
  `evidence/T001-….md（产出：index.html、styles.css、script.js）`）；
- 回答 **n**（或直接回车）→ 任务停在"**进行中**"，它会把"以后怎么标完成"的命令递给你；
- `--yes` 只跳过"要不要写入"，**不会替你判完成**（默认判"没完成"）——"做完"这个判断必须有人点头；
- 想连这一步也自动化，用 `--done`（写入后直接记完成）。

还有一条你会立刻感到的变化：**产出过的任务不再显示"未开始"**。以前它是"未开始"，于是所有依赖它的任务都被挡住，而它自己又被"重复执行"拦着——两头堵死。现在产出即推进为"进行中"，你只要确认完成（上面那一步，或 `track`），后面的任务就能接着做。

### 6.2 记进展、可反悔

```powershell
pm-agent track "T003 做完了，提交 abc123" --path 我的项目
```

一句话更新状态。它会先回显"我听懂的是：任务 / 状态 / 证据"，等你确认才写——归属判断是规则式的（先认编号、再按标题相似度），所以认不准时它会问，而不是猜。加 `--yes` 跳过确认。

```powershell
pm-agent history 我的项目                          # 变更记录（最近在前）
pm-agent history 我的项目 --requirement FR-003     # 某条需求的来龙去脉（出现 / 修改 / 无记录）
pm-agent undo 我的项目                             # 撤回最近一次变更
```

撤回是**逐字节**回到改动前（连换行风格都一致），不是"差不多回去"。

**但它是整份文件的快照，不是补丁**：`undo` 把那份文件恢复到"那次写入之前"的样子。
所以如果你在那次写入**之后**又手工改过同一份文件，`undo` 会把手改的内容一起退回去
（真实踩过：撤回一次旧记录，把之后新写进 `tasks.md` 的三段任务一并退没了）。
要撤很久以前的一次改动时，先 `pm-agent history 我的项目` 看清有哪些记录、哪些文件，
必要时先把当前文件另存一份，再动手。

---

## 7. 风险与汇报

```powershell
pm-agent risks 我的项目                            # 五类风险，每条标"事实"或"推测"
pm-agent risks 我的项目 --all                      # 连已处置的也列出来
pm-agent risks 我的项目 --ack "范围蔓延:规范"       # 标记某条已处置，之后不再重复提醒

pm-agent report 我的项目                           # 给自己看的进度汇报
pm-agent report 我的项目 --audience 上级           # 换读者：自己 / 团队 / 上级
pm-agent report 我的项目 --since 2026-09-16        # 只看这个周期
pm-agent report 我的项目 --write                   # 同时存一份到 reports/
```

汇报里每条都带来源（哪个文件的哪一行）；**没有来源的东西写不进去**——这是它对抗"看起来在推进"的方式。

另外两条复盘用的：

```powershell
pm-agent goals 我的项目                            # 对照"我想学到什么"和已产出的东西（匹配结果标"推测"）
pm-agent design 我的项目                           # 导出设计说明：每个阶段的取舍与被放弃的备选
```

---

## 8. 能力单元：把做法沉淀下来复用

```powershell
pm-agent skills 我的项目                                       # 列出能力单元（只看元数据）
pm-agent skills 我的项目 --suggest "帮我评审一下这份需求规范"     # 按意思找：同一段意思换三种说法都认得出
pm-agent skill requirement-review --path 我的项目               # 看正文
pm-agent skill requirement-review --path 我的项目 --why         # 看"为什么这样做"
pm-agent skill-use requirement-review --outcome accepted --path 我的项目  # 记一次采纳（declined 则是拒绝）
```

内置四个：需求评审、风险复盘、周报汇总、完成前验证。拒绝一次，两周内它不会再提同一件事。

**三级加载**是这套东西最省上下文的地方：进上下文的只有元数据（它是什么、什么时候用），正文要用时才读，引用文件再往下一层。四个内置单元目前都只用前两级；想要第三级的例子，给自己沉淀的单元附材料就行——在 `skills/<名字>/reference/` 下放文件，然后 `pm-agent skill <名字> --path 我的项目 -r <文件名>` 读它（文件名要真实存在，否则它会告诉你没有）。

自己沉淀一个（要素缺一写不进去）：

```powershell
pm-agent skill-save --name release-check --desc "发版前过一遍" --when "准备发版时" --not-when "只是改了文案" --inputs "变更清单" --outputs "检查结论" --why "发版事故多半出在没人复核的小改动" --step "先看变更范围" --path 我的项目
```

已经有同名单元时，加 `--update` 改成修订（使用历史不会被抹掉）。

跨项目复制（能力共享，数据不共享）：

```powershell
pm-agent skill-share requirement-review --from 项目甲 --to 项目乙
pm-agent skill-share requirement-review --from 项目甲 --to 项目乙 --as 需求复核
```

---

## 9. 多项目并行

```powershell
pm-agent projects 项目架                            # 扫一个目录下的所有项目（只读）
pm-agent projects 项目架 --detail                   # 更细一层
```

项目之间是隔离的：各自的规范、任务、交接记录互不影响。想切换项目，就在命令里点名那个目录。

---

## 10. 搬走、留痕、讲解

```powershell
pm-agent export 我的项目 --to ..\备份              # 整目录复制（不打包）
pm-agent export 我的项目 --to ..\演示副本 --for-demo  # 导出一份当演示用的独立副本
pm-agent restore ..\备份 --to ..\另一台机器        # 从导出恢复，并校验能不能正常打开

pm-agent decide --title "用文件而不是数据库" --background "数据要能直接打开、手工改、整个带走" --option "SQLite" --option "一堆 Markdown/YAML 文件" --choice "文件" --why "可读可改可带走，代价是校验与解析要自己写" --path 我的项目

pm-agent handoff 我的项目                          # 现在就把这一轮的状态写成交接记录
pm-agent handoff 我的项目 --yes                    # 跳过确认

pm-agent stage specify --explain                   # 某个阶段的来龙去脉：目的 / 输入 / 产出 / 依据 / 取舍
pm-agent stage track --explain                     # 阶段名只有 specify / plan / tasks / track
pm-agent guide                                     # 重置一下记忆：从头到尾怎么走
```

`decide` 的五个要素（标题、背景、选择、理由，外加可重复的备选方案）**少一样都写不进去**——决策记录的价值就在于它完整。写正文时**把任务/需求编号写进去**（例如"对应 T077/FR-052"），`trace` 才找得到它。

---

## 11. 一份典型节奏（照着套）

| 时间 | 你做什么 | 敲什么 |
| --- | --- | --- |
| 开工 | 建项目、写清目标与学习目标 | `pm-agent init 我的项目 --name … --goal …` |
| 第 1 天 | 把想法整理成规范，逐条确认、导出评审稿 | `pm-agent specify 我的项目` → `questions` → `confirm FR-002 --path 我的项目` → `review 我的项目` |
| 规范里出现疑问 | 把结论回填进去，别让问题一直挂着 | `pm-agent questions 我的项目` → `pm-agent clarify "结论" --for 序号或来源 --path 我的项目` |
| 第 2 天 | 拆任务，看覆盖缺口，挑出先做哪几条 | `pm-agent breakdown 我的项目` → `tasks 我的项目 --coverage` → `tasks 我的项目 --ready` |
| 拿到一条任务 | 让 AI 先做一稿，你审完再落地 | `pm-agent work T003 --path 我的项目` |
| 每天 | 先接上状态，做完一句 `track`，收工留交接记录 | `pm-agent resume 我的项目` → `pm-agent track "T005 做完了，提交 abc123" --path 我的项目` → `pm-agent handoff 我的项目` |
| 改需求前 | 先看波及面，再决定要不要重评审 | `pm-agent impact FR-007 --path 我的项目` |
| 每周 | 看风险、出汇报、对照学习目标 | `pm-agent risks 我的项目` → `pm-agent report 我的项目 --audience 上级` → `pm-agent goals 我的项目` |
| 遇到关键取舍 | 立刻留一份决策记录 | `pm-agent decide --title … --path 我的项目` |
| 换个做法做熟了 | 沉淀成能力单元，下次让程序提醒你 | `pm-agent skill-save … --path 我的项目` |
| 改错了 | 撤回，看清恢复了什么 | `pm-agent undo 我的项目` |

---

## 12. 命令速查表（37 条）

**要不要项目目录**这一列：`位置` = 第一个参数写项目目录；`--path` = 用 `--path`（或 `-C`）给；`—` = 不需要。

| 命令 | 干什么 | 主要参数 | 项目目录 |
| --- | --- | --- | --- |
| `version` | 显示版本号 | — | — |
| `init` | 建一个空项目（已存在文件不覆盖） | `--name` `--goal` `--learning-goal` `--no-git` | 位置 |
| `show` | 项目概览 | — | 位置 |
| `check` | 体检工作区格式 | `--fix` 补建数据目录 | 位置 |
| `ask` | 调一次模型，验证接入 | `--provider` `--system` | — |
| `stage` | 讲一个阶段的目的/输入/产出/依据 | `--explain` 加取舍与备选 | — |
| `specify` | 目标 → 规范（预览后写入） | `--goal` `--provider` `--yes` | 位置 |
| `breakdown` | 规范 → 任务（预览后写入） | `--provider` `--yes` | 位置 |
| `work` | **让 AI 做掉一条任务**：装配上下文 → 产出文件并**直接写进项目** → **当场问一次"算完成了吗"** | `--provider` `--yes`（只跳过写入确认）`--done`（直接记完成）`--redo`（重复执行的放行开关）；任务编号是位置参数（不写就自动挑） | `--path` |
| `requirements` | 列出需求条目 | `--limit` | 位置 |
| `questions` | 列出待澄清 | — | 位置 |
| `clarify` | **把一处待澄清的结论回填进规范**（问题留着 + 追加结论） | `--for` 指定序号或来源（只剩一条可省） | `--path` |
| `confirm` | 确认一条需求看过了 | 需求编号是位置参数 | `--path` |
| `review` | 导出评审稿 | — | 位置 |
| `tasks` | 列任务：全部 / 能动手 / 覆盖缺口 | `--ready` `--milestone` `--coverage` | 位置 |
| `resume` | 重建状态摘要（**开工第一件事**） | — | 位置 |
| `track` | 一句话更新任务状态 | `--yes`；那句话是位置参数 | `--path` |
| `handoff` | 写一份交接记录 | `--yes` | 位置 |
| `history` | 变更记录 | `--limit` `--requirement` | 位置 |
| `undo` | 撤回最近一次变更 | — | 位置 |
| `risks` | 五类风险（事实/推测分开） | `--all` `--ack` | 位置 |
| `report` | 四类分组的汇报 | `--audience` `--since` `--write` | 位置 |
| `goals` | 学习目标对照 | — | 位置 |
| `design` | 导出设计说明 | — | 位置 |
| `skills` | 列能力单元 / 按意思找 | `--suggest` | 位置 |
| `skill` | 看一个能力单元 | `--why` `--reference`；名字是位置参数 | `--path` |
| `skill-save` | 沉淀/修订一个能力单元 | `--name` `--desc` `--when` `--not-when` `--inputs` `--outputs` `--why` `--step` `--update` | `--path` |
| `skill-use` | 记一次采纳/拒绝 | `--outcome` | `--path` |
| `skill-share` | 把能力单元复制到另一个项目 | `--from` `--to` `--as` | — |
| `projects` | 跨项目总览（只读） | `--detail` | 位置（放项目架目录） |
| `export` | 整个项目复制到别处 | `--to` `--for-demo` | 位置 |
| `restore` | 从导出恢复 | `--to` | 位置（放导出目录） |
| `demo` | 建示范项目 | `--reset`（只认自建示范项目） | 位置 |
| `guide` | 第一次用？照着走一遍 | — | — |
| `impact` | 改需求前先看波及面 | 需求编号是位置参数 | `--path` |
| `trace` | 任务 → 需求 → 决策 | 任务编号是位置参数 | `--path` |
| `decide` | 记录一次关键决定 | `--title` `--background` `--option` `--choice` `--why` | `--path` |

任何一条后面加 `--help`，它会列出自己的参数——那是最可靠的一手说明（`pm-agent --help` 看全部命令）。

---

## 13. 卡住了怎么办

| 报错 / 现象 | 真正的原因 | 怎么办 |
| --- | --- | --- |
| `错误 项目工作区有问题，无法打开：project.yaml：缺少项目元信息文件` | 你当前不在项目目录里，或者没点名项目 | 加上项目目录（`pm-agent show 我的项目`），或改用 `--path`。项目目录指的是有 `project.yaml` 的那一层 |
| `No such option: --path` | 这条命令的项目目录是**位置参数**（如 `review`、`questions`、`show`） | 去掉 `--path`，直接写 `pm-agent review 我的项目`；拿不准就看 `--help` 里有没有 `--path` |
| `错误 T002 的依赖还没完成，不能动手` | 想跳过依赖先做后面那条 | 先把被依赖的那条做掉（`pm-agent tasks 我的项目 --ready` 告诉你现在能动手的是哪些）；如果**被依赖的那条其实已经产出过**，提示里会直接说"它已经有产出了（时间：文件），做完就 `pm-agent track …`"——照着敲一句即可解锁 |
| 有产出的任务却显示"未开始" | 旧版本的行为（产出不改状态） | 现在不会了：产出即推进为"进行中"（FR-056）。若你的项目是旧版本留下的这种状态，用提示里的 `pm-agent track` 把它标完成 |
| `错误 现在没有能动手的任务` | 剩下的都被依赖卡住或都已经完成 | 看看 `pm-agent tasks 我的项目` 全貌；真是做完就该 `handoff` 收尾了 |
| `错误 模型产出的交付物不合格` | 产出漏了"做法/交付物/验证/没做什么"中的某一节，或是客套话/回声输出 | 报错里附了模型原文；重跑一次通常就好，反复失败就改 `prompts/work.user.md` |
| `错误 交付物里没有任何文件块` | 模型只写了一堆说明，没给出要落盘的文件 | 同上；`PROMPTS/work.user.md` 里写死了文件块的格式：`### 文件：相对路径` + 围栏 |
| `错误 不许由 work 直接改「spec.md」` | 产出想改程序自己维护的文件 | 那是**保护名单**（`project.yaml` / `spec.md` / `plan.md` / `tasks.md`、`history/`）。规范用 `specify` 或手工改，任务用 `breakdown`；`work` 只写它自己交付的文件 |
| `错误「../外面.py」想往项目外面写` | 产出给了个越界路径 | 也是保护：`work` 只写项目目录内的相对路径。让模型重跑，或手工把内容放到项目里 |
| `错误 现在有 2 处待澄清，得说清回答的是哪一处` | `clarify` 没写 `--for`，而未决问题不止一条 | 加上 `--for`（序号或来源都行）：`pm-agent clarify "结论" --for FR-003 --path 我的项目` |
| `错误 没有来自「FR-999」的待澄清问题` | 来源写错了 | `pm-agent questions 我的项目` 看一眼真实的来源与序号（提示里也会列出来） |
| `错误 T001 之前已经产出过（最近一次 …），这次没有重做` | 同一条任务跑了第二遍 | 这不是故障，是拦住你堆出第二套互相冲突的文件。想重来先 `pm-agent undo 我的项目`；确实要叠加就加 `--redo` |
| `我听懂的是` 里的**任务标题不是你以为的那条** | `track` / `work` 的项目目录是 `--path`，抄漏了就打到别的项目上（当前目录那个） | 取消（回 `n`），补上 `--path 我的项目` 再来。两条命令的回显第一行现在都会打印"项目：<名称>"，确认前先看一眼 |
| 撤回后项目里多了空目录 | —— | 现在不会了：一次 `undo` 会把这次新建的目录一起收走（`history/` 里那条记录是留痕，属于正常） |
| `CERTIFICATE_VERIFY_FAILED` | MSYS2 版 Python 没有系统 CA | 设 `PM_AGENT_CA_BUNDLE`（第 0 节） |
| `PM_AGENT_MODEL_API_KEY 是空的` | 没设密钥 | 设密钥，或加 `--provider echo` 走离线 |
| `HTTP 401` | 密钥无效 | 换一个有效密钥 |
| `错误 没有名为 '乱写的' 的阶段` | 阶段名写错了 | 只有 `specify` / `plan` / `tasks` / `track` |
| `错误 . 里已经有东西了` | 目标目录非空 | 换个空目录；`--reset` 只对**本工具建的示范项目**有效 |
| `错误 拒绝清空 …：它不是本工具建的示范项目` | 想用 `--reset` 清一个真实项目 | 这是保护：命令不做这件事。真要清空请自己删目录 |
| `无法将"pm-agent"项识别为…` | 没激活虚拟环境 | 先 `& ".\.venv\bin\Activate.ps1"`；或者写全路径 `.\.venv\bin\pm-agent.exe` |
| 敲完命令没反应、光标一直等 | 它在等你确认（`[Y/n]`） | 敲 `y` 回车；不想被问就加 `--yes` |

---

## 14. 一句话记住三件事

1. **新窗口先激活 + 设密钥和 CA**，然后 `pm-agent ask "在吗"` 确认接上了。
2. **每条命令都要点名项目目录**——数据在项目里，不在程序里。
3. **写入永远先给预览**，不满意就 `undo`；拿不准就加 `--help`。
4. **让 AI 干活用 `work`，判完成仍是你的活**——它给草稿，你给证据。
