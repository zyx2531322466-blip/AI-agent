请接下这条任务，并产出它的交付物。

## 项目

- 名称：{{name}}
- 一句话目标：{{goal}}
- 这个项目想学到什么：{{learning_goals}}

## 这次要做的任务

- 编号：{{task_id}}
- 标题：{{task_title}}
- 里程碑：{{task_milestone}}
- 引用（需求 / 成功标准）：{{task_reference}}
- 完成标准：{{task_standard}}
- 优先级：{{task_priority}}
- 依赖：{{task_depends}}
- 当前状态：{{task_status}}

## 它的来源需求（围绕这些做事，别跑题）

{{requirements}}

## 相关决策（这个项目已经定过的调子，别绕回去重开）

{{decisions}}

## 相关能力单元的做法（能用上就用上，并在"做法"里点出来）

{{skills}}

## 这条任务以前做过吗

{{previous_files}}

（如果上面列出了文件：**沿用同一套文件**——改它们，而不是另起一套名字。
真有必要换布局，就在第 1 节里说明理由，否则项目里会并存两份互相冲突的实现。）

## 输出格式

# {{task_id}} 交付物

## 1. 做法
（打算怎么做：要动哪些文件、按什么顺序、为什么这么做）

## 2. 交付物
（**每个文件一个文件块**，写成下面这样——程序按这个格式把内容直接落进项目目录：

### 文件：todo.py

```python
# 这里放这个文件的完整内容
```

### 文件：docs/调研笔记.md

```markdown
# 这里放这个文件的完整内容
```

规则：路径是**项目内的相对路径**（可以建子目录）；不要写绝对路径、不要出现 `..`；
不要写 `spec.md` / `tasks.md` / `plan.md` / `project.yaml`（那四个归别的命令管）；
同一个路径只出现一次；内容必须是完整可用的最终形态，不留空壳。）

## 3. 怎么验证
（做完之后怎么确认它对得上完成标准：跑什么、看什么、什么算通过）

## 4. 没做什么
（没做的事、没把握的地方、需要使用者拍板的地方；确实没有就写"无"）
