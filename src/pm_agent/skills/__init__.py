"""内置能力单元（作为包数据随程序一起分发）。

一个能力单元 = 一个目录 + 一份 ``SKILL.md``（YAML frontmatter + 正文步骤）。
项目自己沉淀的能力单元放在 ``<项目>/skills/`` 下，格式相同。

**加载逻辑不在这里**：读写、匹配、沉淀都在 ``workspace/skills.py``，
这个包只负责装着内容——内容归内容，逻辑归逻辑。
"""
